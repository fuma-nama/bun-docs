diff --git b/content/docs/bundler/esbuild.mdx a/content/docs/bundler/esbuild.mdx
index b9845712..44cfbf50 100644
--- b/content/docs/bundler/esbuild.mdx
+++ a/content/docs/bundler/esbuild.mdx
@@ -30,7 +30,7 @@ Bun and esbuild both provide a command-line interface.
 
 ```bash title="terminal"
 # esbuild
-esbuild <entrypoint> --outdir=out --bundle
+$ esbuild <entrypoint> --outdir=out --bundle
 
 # bun
 $ bun build <entrypoint> --outdir=out
diff --git b/content/docs/bundler/executables.mdx a/content/docs/bundler/executables.mdx
index c72c6093..6f7584be 100644
--- b/content/docs/bundler/executables.mdx
+++ a/content/docs/bundler/executables.mdx
@@ -498,10 +498,10 @@ you shouldn't see this
 However, with the `BUN_BE_BUN=1` environment variable, it acts just like the `bun` binary:
 
 ```bash title="terminal"
-# With the env var, the executable acts like the `bun` CLI
+With the env var, the executable acts like the `bun` CLI
 $ BUN_BE_BUN=1 ./such-bun install
 
-bun install v1.2.16-canary.1 (1d1db811)
+bun install v$BUN_LATEST_VERSION (1d1db811)
 Checked 63 installs across 64 packages (no changes) [5.00ms]
 ```
 
diff --git b/content/docs/bundler/fullstack.mdx a/content/docs/bundler/fullstack.mdx
index 50fb209d..357f624a 100644
--- b/content/docs/bundler/fullstack.mdx
+++ a/content/docs/bundler/fullstack.mdx
@@ -123,7 +123,7 @@ Becomes something like this:
 
 To use React in your client-side code, import `react-dom/client` and render your app.
 
-```ts title="src/backend.ts"
+```ts tab="src/backend.ts"
 import dashboard from "../public/dashboard.html";
 import { serve } from "bun";
 
@@ -138,7 +138,7 @@ serve({
 });
 ```
 
-```tsx title="src/frontend.tsx"
+```tsx tab="src/frontend.tsx"
 import { createRoot } from "react-dom/client";
 import App from "./app";
 
@@ -147,7 +147,7 @@ const root = createRoot(container!);
 root.render(<App />);
 ```
 
-```html title="public/dashboard.html"
+```html tab="public/dashboard.html"
 <!DOCTYPE html>
 <html>
   <head>
@@ -161,7 +161,7 @@ root.render(<App />);
 </html>
 ```
 
-```tsx title="src/app.tsx"
+```tsx tab="src/app.tsx"
 import { useState } from "react";
 
 export default function App() {
@@ -519,7 +519,7 @@ Bun uses `HTMLRewriter` to scan for `<script>` and `<link>` tags in HTML files,
 <Steps>
 <Step>
 
-  ### 1. <script> Processing
+  ### 1. `<script>` Processing
 - Transpiles TypeScript, JSX, and TSX in `<script>` tags
 - Bundles imported dependencies
 - Generates sourcemaps for debugging
@@ -785,7 +785,7 @@ export function App() {
 }
 ```
 
-```scss title="src/styles.css"
+```css title="src/styles.css"
 * {
   margin: 0;
   padding: 0;
diff --git b/content/docs/bundler/html-static.mdx a/content/docs/bundler/html-static.mdx
index 4a5b970d..2c9bb61f 100644
--- b/content/docs/bundler/html-static.mdx
+++ a/content/docs/bundler/html-static.mdx
@@ -22,9 +22,7 @@ To get started, pass HTML files to `bun`.
 
 ```bash title="terminal"
 $ bun ./index.html
-```
 
-```
 Bun v$BUN_LATEST_VERSION
 ready in 6.62ms
 → http://localhost:3000/
@@ -48,9 +46,7 @@ When you pass a single `.html` file to Bun, Bun will use it as a fallback route
 
 ```bash title="terminal"
 $ bun index.html
-```
 
-```
 Bun v$BUN_LATEST_VERSION
 ready in 6.62ms
 → http://localhost:3000/
@@ -99,9 +95,7 @@ To specify multiple files, you can use glob patterns that end in `.html`:
 
 ```bash title="terminal"
 $ bun ./**/*.html
-```
 
-```
 Bun v$BUN_LATEST_VERSION
 ready in 6.62ms
 → http://localhost:3000/
@@ -117,9 +111,7 @@ The base path is chosen from the longest common prefix among all the files.
 
 ```bash title="terminal"
 $ bun ./index.html ./about/index.html ./about/foo/index.html
-```
 
-```
 Bun v$BUN_LATEST_VERSION
 ready in 6.62ms
 → http://localhost:3000/
@@ -160,7 +152,7 @@ For example:
 }
 ```
 
-```scss tab="abc.css"
+```css tab="abc.css"
 body {
   background-color: red;
 }
@@ -168,7 +160,7 @@ body {
 
 This outputs:
 
-```scss title="styles.css"
+```css title="styles.css"
 body {
   background-color: red;
 }
@@ -182,7 +174,7 @@ body {
 
 You can reference local assets in your CSS files.
 
-```scss title="styles.css"
+```css title="styles.css"
 body {
   background-image: url("./logo.png");
 }
@@ -190,7 +182,7 @@ body {
 
 This will copy `./logo.png` to the output directory and rewrite the path in the CSS file to include a content hash.
 
-```scss title="styles.css"
+```css title="styles.css"
 body {
   background-image: url("./logo-[ABC123].png");
 }
@@ -229,30 +221,16 @@ plugins = ["bun-plugin-tailwind"]
 
 Then, reference TailwindCSS in your HTML via `<link>` tag, `@import` in CSS, or import in JavaScript.
 
-<Tabs items={['index.html', 'styles.css', 'app.ts']}>
-  <Tab value="index.html">
-
-    ```html title="index.html"
-    <!-- Reference TailwindCSS in your HTML -->
-    <link rel="stylesheet" href="tailwindcss" />
-   ```
-
-  </Tab>
-  <Tab value="styles.css">
-
-    ```scss title="styles.css"
-    @import "tailwindcss";
-   ```
-
-  </Tab>
-  <Tab value="app.ts">
-
-    ```ts title="app.ts"
-    import "tailwindcss";
-   ```
-
-  </Tab>
-</Tabs>
+```html tab="index.html"
+<!-- Reference TailwindCSS in your HTML -->
+<link rel="stylesheet" href="tailwindcss" />
+```
+```scss tab="styles.css"
+@import "tailwindcss";
+```
+```ts tab="app.ts"
+import "tailwindcss";
+```
 
 <Callout type="info">Only one of those are necessary, not all three.</Callout>
 
@@ -351,9 +329,7 @@ To enable, pass the `--console` CLI flag.
 
 ```bash title="terminal"
 $ bun ./index.html --console
-```
 
-```
 Bun v$BUN_LATEST_VERSION
 ready in 6.62ms
 → http://localhost:3000/
@@ -380,22 +356,16 @@ While the server is running:
 
 When you're ready to deploy, use `bun build` to create optimized production bundles:
 
-<Tabs items={['CLI', 'API']}>
-  <Tab value="CLI">
-    ```bash title="terminal"
-    $ bun build ./index.html --minify --outdir=dist
-   ```
-  </Tab>
-  <Tab value="API">
-    ```ts title="build.ts"
-    await Bun.build({
-      entrypoints: ["./index.html"],
-      outdir: "./dist",
-      minify: true,
-    });
-   ```
-  </Tab>
-</Tabs>
+```bash title="terminal" tab="CLI"
+$ bun build ./index.html --minify --outdir=dist
+```
+```ts title="build.ts" tab="API"
+await Bun.build({
+	entrypoints: ["./index.html"],
+	outdir: "./dist",
+	minify: true,
+});
+```
 
 <Callout type="warning">
   Currently, plugins are only supported through `Bun.build`'s API or through `bunfig.toml` with the frontend dev server
diff --git b/content/docs/bundler/index.mdx a/content/docs/bundler/index.mdx
index dbcc5aaf..494e6551 100644
--- b/content/docs/bundler/index.mdx
+++ a/content/docs/bundler/index.mdx
@@ -77,8 +77,8 @@ To create our bundle:
   <Tab value="JavaScript">
     ```ts title="build.ts"
     await Bun.build({
-      entrypoints: ["./index.tsx"],
-      outdir: "./out",
+    	entrypoints: ['./index.tsx'],
+    	outdir: './out',
     });
    ```
   </Tab>
@@ -154,15 +154,48 @@ Like the Bun runtime, the bundler supports an array of file types out of the box
 | Extensions                                            | Details                                                                                                                                                                                                                                                                                                                                                      |
 | ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
 | `.js` `.jsx` `.cjs` `.mjs` `.mts` `.cts` `.ts` `.tsx` | Uses Bun's built-in transpiler to parse the file and transpile TypeScript/JSX syntax to vanilla JavaScript. The bundler executes a set of default transforms including dead code elimination and tree shaking. At the moment Bun does not attempt to down-convert syntax; if you use recently ECMAScript syntax, that will be reflected in the bundled code. |
-| `.json`                                               | JSON files are parsed and inlined into the bundle as a JavaScript object.<br/><br/>`js<br/>import pkg from "./package.json";<br/>pkg.name; // => "my-package"<br/>`                                                                                                                                                                                          |
-| `.jsonc`                                              | JSON with comments. Files are parsed and inlined into the bundle as a JavaScript object.<br/><br/>`js<br/>import config from "./config.jsonc";<br/>config.name; // => "my-config"<br/>`                                                                                                                                                                      |
-| `.toml`                                               | TOML files are parsed and inlined into the bundle as a JavaScript object.<br/><br/>`js<br/>import config from "./bunfig.toml";<br/>config.logLevel; // => "debug"<br/>`                                                                                                                                                                                      |
-| `.yaml` `.yml`                                        | YAML files are parsed and inlined into the bundle as a JavaScript object.<br/><br/>`js<br/>import config from "./config.yaml";<br/>config.name; // => "my-app"<br/>`                                                                                                                                                                                         |
-| `.txt`                                                | The contents of the text file are read and inlined into the bundle as a string.<br/><br/>`js<br/>import contents from "./file.txt";<br/>console.log(contents); // => "Hello, world!"<br/>`                                                                                                                                                                   |
+| `.json`                                               | JSON files are parsed and inlined into the bundle as a JavaScript object. <include>index.mdx#content-types:json</include>                                                                                                                                                                                                                                    |
+| `.jsonc`                                              | JSON with comments. Files are parsed and inlined into the bundle as a JavaScript object. <include>index.mdx#content-types:jsonc</include>                                                                                                                                                                                                                                    |
+| `.toml`                                               | TOML files are parsed and inlined into the bundle as a JavaScript object. <include>index.mdx#content-types:toml</include>                                                                                                                                                                                                                                    |
+| `.yaml` `.yml`                                        | YAML files are parsed and inlined into the bundle as a JavaScript object. <include>index.mdx#content-types:yaml</include>                                                                                                                                                                                                                                    |
+| `.txt`                                                | The contents of the text file are read and inlined into the bundle as a string.<include>index.mdx#content-types:txt</include>                                                                                                                                                                                                                                |
 | `.html`                                               | HTML files are processed and any referenced assets (scripts, stylesheets, images) are bundled.                                                                                                                                                                                                                                                               |
 | `.css`                                                | CSS files are bundled together into a single `.css` file in the output directory.                                                                                                                                                                                                                                                                            |
 | `.node` `.wasm`                                       | These files are supported by the Bun runtime, but during bundling they are treated as assets.                                                                                                                                                                                                                                                                |
 
+{/* start llm ignored codeblock */}
+<section id="content-types:json" style={{display:"none"}}>
+```js
+import pkg from "./package.json";
+pkg.name; // => "my-package"
+```
+</section>
+<section id="content-types:jsonc" style={{display:"none"}}>
+```js
+import config from "./config.jsonc";
+config.name; // => "my-config"
+```
+</section>
+<section id="content-types:toml" style={{display:"none"}}>
+```js
+import config from "./bunfig.toml";
+config.logLevel; // => "debug"
+```
+</section>
+<section id="content-types:yaml" style={{display:"none"}}>
+```js
+import config from "./config.yaml";
+config.name; // => "my-app"
+```
+</section>
+<section id="content-types:txt" style={{display:"none"}}>
+```js
+import contents from "./file.txt";
+console.log(contents); // => "Hello, world!"
+```
+</section>
+{/* end llm ignored codeblock */}
+
 ### Assets
 
 If the bundler encounters an import with an unrecognized extension, it treats the imported file as an external file. The referenced file is copied as-is into `outdir`, and the import is resolved as a path to the file.
@@ -348,28 +381,30 @@ The intended execution environment for the bundle.
 
 Depending on the target, Bun will apply different module resolution rules and optimizations.
 
-<Card title="browser" icon="globe">
-  **Default.** For generating bundles that are intended for execution by a browser. Prioritizes the `"browser"` export
-  condition when resolving imports. Importing any built-in modules, like `node:events` or `node:path` will work, but
-  calling some functions, like `fs.readFile` will not work.
-</Card>
-
-<Card title="bun" icon="server">
-For generating bundles that are intended to be run by the Bun runtime. In many cases, it isn't necessary to bundle server-side code; you can directly execute the source code without modification. However, bundling your server code can reduce startup times and improve running performance. This is the target to use for building full-stack applications with build-time HTML imports, where both server and client code are bundled together.
+<Cards className="grid-cols-1">
+  <Card title="Browser (default)">
+    For generating bundles that are intended for execution by a browser. Prioritizes the
+    `"browser"` export condition when resolving imports. Importing any built-in modules, like
+    `node:events` or `node:path` will work, but calling some functions, like `fs.readFile` will not
+    work.
+  </Card>
 
-All bundles generated with `target: "bun"` are marked with a special `// @bun` pragma, which indicates to the Bun runtime that there's no need to re-transpile the file before execution.
+  <Card title="Bun">
+    For generating bundles that are intended to be run by the Bun runtime. In many cases, it isn't necessary to bundle server-side code; you can directly execute the source code without modification. However, bundling your server code can reduce startup times and improve running performance. This is the target to use for building full-stack applications with build-time HTML imports, where both server and client code are bundled together.
 
-If any entrypoints contains a Bun shebang (`#!/usr/bin/env bun`) the bundler will default to `target: "bun"` instead of `"browser"`.
+    All bundles generated with `target: "bun"` are marked with a special `// @bun` pragma, which indicates to the Bun runtime that there's no need to re-transpile the file before execution.
 
-When using `target: "bun"` and `format: "cjs"` together, the `// @bun @bun-cjs` pragma is added and the CommonJS wrapper function is not compatible with Node.js.
+    If any entrypoints contains a Bun shebang (`#!/usr/bin/env bun`) the bundler will default to `target: "bun"` instead of `"browser"`.
 
-</Card>
+    When using `target: "bun"` and `format: "cjs"` together, the `// @bun @bun-cjs` pragma is added and the CommonJS wrapper function is not compatible with Node.js.
+  </Card>
 
-<Card title="node" icon="node">
-  For generating bundles that are intended to be run by Node.js. Prioritizes the `"node"` export condition when
-  resolving imports, and outputs `.mjs`. In the future, this will automatically polyfill the Bun global and other
-  built-in `bun:*` modules, though this is not yet implemented.
-</Card>
+  <Card title="Node">
+    For generating bundles that are intended to be run by Node.js. Prioritizes the `"node"` export
+    condition when resolving imports, and outputs `.mjs`. In the future, this will automatically
+    polyfill the Bun global and other built-in `bun:*` modules, though this is not yet implemented.
+  </Card>
+</Cards>
 
 ### format
 
@@ -435,21 +470,21 @@ Configure JSX transform behavior. Allows fine-grained control over how JSX is co
   <Tab value="JavaScript">
     ```ts title="index.ts"
     await Bun.build({
-      entrypoints: ["./app.tsx"],
-      outdir: "./out",
+      entrypoints: ['./app.tsx'],
+      outdir: './out',
       jsx: {
-        factory: "h",
-        fragment: "Fragment",
-        runtime: "classic",
+        factory: 'h',
+        fragment: 'Fragment',
+        runtime: 'classic',
       },
     });
-   ```
+       ```
   </Tab>
   <Tab value="CLI">
     ```bash title="terminal"
     # JSX configuration is handled via bunfig.toml or tsconfig.json
     $ bun build ./app.tsx --outdir ./out
-   ```
+       ```
   </Tab>
 </Tabs>
 
@@ -459,20 +494,20 @@ Configure JSX transform behavior. Allows fine-grained control over how JSX is co
   <Tab value="JavaScript">
     ```ts title="index.ts"
     await Bun.build({
-      entrypoints: ["./app.tsx"],
-      outdir: "./out",
-      jsx: {
-        importSource: "preact",
-        runtime: "automatic",
-      },
+    	entrypoints: ['./app.tsx'],
+    	outdir: './out',
+    	jsx: {
+    		importSource: 'preact',
+    		runtime: 'automatic',
+    	},
     });
-   ```
+      ```
   </Tab>
   <Tab value="CLI">
     ```bash title="terminal"
     # JSX configuration is handled via bunfig.toml or tsconfig.json
     $ bun build ./app.tsx --outdir ./out
-   ```
+       ```
   </Tab>
 </Tabs>
 
@@ -526,7 +561,7 @@ To bundle `entry-a.ts` and `entry-b.ts` with code-splitting enabled:
   <Tab value="CLI">
     ```bash title="terminal"
     $ bun build ./entry-a.ts ./entry-b.ts --outdir ./out --splitting
-   ```
+    ```
   </Tab>
 </Tabs>
 
@@ -582,7 +617,7 @@ Injects environment variables into the bundled output by converting `process.env
   <Tab value="CLI">
     ```bash title="terminal"
     $ bun build ./index.tsx --outdir ./out --env inline
-   ```
+    ```
   </Tab>
 </Tabs>
 
@@ -612,7 +647,7 @@ Inlines environment variables matching the given prefix (the part before the `*`
     await Bun.build({
       entrypoints: ['./index.tsx'],
       outdir: './out',
-      
+
       // Inline all env vars that start with "ACME_PUBLIC_"
       env: "ACME_PUBLIC_*",
     })
@@ -628,7 +663,7 @@ Inlines environment variables matching the given prefix (the part before the `*`
 For example, given the following environment variables:
 
 ```bash title="terminal"
-$ FOO=bar BAZ=123 ACME_PUBLIC_URL=https://acme.com
+FOO=bar BAZ=123 ACME_PUBLIC_URL=https://acme.com
 ```
 
 And source code:
@@ -939,9 +974,9 @@ When a string is provided for the `naming` field, it is used only for bundles th
   <Tab value="CLI">
     ```bash title="terminal"
     $ bun build ./index.tsx --outdir ./out \
-      --entry-naming '[dir]/[name].[ext]' \
-      --chunk-naming '[name]-[hash].[ext]' \
-      --asset-naming '[name]-[hash].[ext]'
+        --entry-naming '[dir]/[name].[ext]' \
+        --chunk-naming '[name]-[hash].[ext]' \
+        --asset-naming '[name]-[hash].[ext]'
    ```
   </Tab>
 </Tabs>
@@ -1475,44 +1510,39 @@ return new Response(artifact);
 
 The Bun runtime implements special pretty-printing of `BuildArtifact` object to make debugging easier.
 
-<Tabs items={['JavaScript', 'CLI']} groupId="js/cli">
-  <Tab value="JavaScript">
-    ```ts title="build.ts"
-    // build.ts
-    const build = await Bun.build({
-      /* */
-    });
+```ts title="build.ts"
+// build.ts
+const build = await Bun.build({
+  /* */
+});
 
-    const artifact = build.outputs[0];
-    console.log(artifact);
-   ```
-  </Tab>
-  <Tab value="CLI">
-    ```bash title="terminal" Shell output
-    $ bun run build.ts
-
-    BuildArtifact (entry-point) {
-      path: "./index.js",
-      loader: "tsx",
-      kind: "entry-point",
-      hash: "824a039620219640",
-      Blob (74756 bytes) {
-        type: "text/javascript;charset=utf-8"
-      },
-      sourcemap: BuildArtifact (sourcemap) {
-        path: "./index.js.map",
-        loader: "file",
-        kind: "sourcemap",
-        hash: "e7178cda3e72e301",
-        Blob (24765 bytes) {
-          type: "application/json;charset=utf-8"
-        },
-        sourcemap: null
-      }
-    }
-   ```
-  </Tab>
-</Tabs>
+const artifact = build.outputs[0];
+console.log(artifact);
+```
+
+```bash title="Shell output"
+$ bun run build.ts
+
+BuildArtifact (entry-point) {
+  path: "./index.js",
+  loader: "tsx",
+  kind: "entry-point",
+  hash: "824a039620219640",
+  Blob (74756 bytes) {
+    type: "text/javascript;charset=utf-8"
+  },
+  sourcemap: BuildArtifact (sourcemap) {
+    path: "./index.js.map",
+    loader: "file",
+    kind: "sourcemap",
+    hash: "e7178cda3e72e301",
+    Blob (24765 bytes) {
+      type: "application/json;charset=utf-8"
+    },
+    sourcemap: null
+  }
+}
+```
 
 ## Bytecode
 
diff --git b/content/docs/bundler/minifier.mdx a/content/docs/bundler/minifier.mdx
index aaedb143..9d687f61 100644
--- b/content/docs/bundler/minifier.mdx
+++ a/content/docs/bundler/minifier.mdx
@@ -102,12 +102,12 @@ Renames local variables and function names to shorter identifiers using frequenc
 
 Converts boolean literals to shorter expressions.
 
-```ts Input
+```ts title="Input"
 true
 false
 ```
 
-```js Output
+```js title="Output"
 !0
 !1
 ```
@@ -118,7 +118,7 @@ false
 
 Simplifies boolean expressions using logical rules.
 
-```ts Input
+```ts title="Input"
 !!x
 x === true
 x && true
@@ -127,7 +127,7 @@ x || false
 !false
 ```
 
-```js Output
+```js title="Output"
 x
 x
 x
@@ -142,28 +142,27 @@ x
 
 Replaces `undefined` with shorter equivalent.
 
-```ts Input
+```ts title="Input"
 undefined
 let x = undefined;
 ```
 
-```js Output
+```js title="Output"
 void 0
 let x=void 0;
 ```
 
 ### Undefined equality optimization
-
 **Mode:** `--minify-syntax`
 
 Optimizes loose equality checks with undefined.
 
-```ts Input
+```ts title="Input"
 x == undefined
 x != undefined
 ```
 
-```js Output
+```js title="Output"
 x == null
 x != null
 ```
@@ -174,12 +173,12 @@ x != null
 
 Converts Infinity to mathematical expressions.
 
-```ts Input
+```ts title="Input"
 Infinity
 -Infinity
 ```
 
-```js Output
+```js title="Output"
 1/0
 -1/0
 ```
@@ -190,7 +189,7 @@ Infinity
 
 Optimizes typeof comparisons and evaluates constant typeof expressions.
 
-```ts Input
+```ts title="Input"
 typeof x === 'undefined'
 typeof x !== 'undefined'
 typeof require
@@ -201,7 +200,7 @@ typeof "str"
 typeof 123n
 ```
 
-```js Output
+```js title="Output"
 typeof x>'u'
 typeof x<'u'
 "function"
@@ -218,7 +217,7 @@ typeof x<'u'
 
 Formats numbers in the most compact representation.
 
-```ts Input
+```ts title="Input"
 10000
 100000
 1000000
@@ -226,7 +225,7 @@ Formats numbers in the most compact representation.
 -42.0
 ```
 
-```js Output
+```js title="Output"
 1e4
 1e5
 1e6
@@ -240,7 +239,7 @@ Formats numbers in the most compact representation.
 
 Evaluates arithmetic operations at compile time.
 
-```ts Input
+```ts title="Input"
 1 + 2
 10 - 5
 3 * 4
@@ -249,7 +248,7 @@ Evaluates arithmetic operations at compile time.
 2 ** 3
 ```
 
-```js Output
+```js title="Output"
 3
 5
 12
@@ -264,7 +263,7 @@ Evaluates arithmetic operations at compile time.
 
 Evaluates bitwise operations at compile time.
 
-```ts Input
+```ts title="Input"
 5 & 3
 5 | 3
 5 ^ 3
@@ -273,7 +272,7 @@ Evaluates bitwise operations at compile time.
 ~5
 ```
 
-```js Output
+```js title="Output"
 1
 7
 6
@@ -288,13 +287,13 @@ Evaluates bitwise operations at compile time.
 
 Combines string literals at compile time.
 
-```ts Input
+```ts title="Input"
 "a" + "b"
 "x" + 123
 "foo" + "bar" + "baz"
 ```
 
-```js Output
+```js title="Output"
 "ab"
 "x123"
 "foobarbaz"
@@ -306,12 +305,12 @@ Combines string literals at compile time.
 
 Evaluates string character access at compile time.
 
-```ts Input
+```ts title="Input"
 "foo"[2]
 "hello"[0]
 ```
 
-```js Output
+```js title="Output"
 "o"
 "h"
 ```
@@ -322,12 +321,12 @@ Evaluates string character access at compile time.
 
 Evaluates template literals with constant expressions.
 
-```ts Input
+```ts title="Input"
 `a${123}b`
 `result: ${5 + 10}`
 ```
 
-```js Output
+```js title="Output"
 "a123b"
 "result: 15"
 ```
@@ -338,13 +337,13 @@ Evaluates template literals with constant expressions.
 
 Converts simple template literals to regular strings.
 
-```ts Input
+```ts title="Input"
 `Hello World`
 `Line 1
 Line 2`
 ```
 
-```js Output
+```js title="Output"
 "Hello World"
 "Line 1\nLine 2"
 ```
@@ -355,13 +354,13 @@ Line 2`
 
 Chooses the optimal quote character to minimize escapes.
 
-```ts Input
+```ts title="Input"
 "It's a string"
 'He said "hello"'
 `Simple string`
 ```
 
-```js Output
+```js title="Output"
 "It's a string"
 'He said "hello"'
 "Simple string"
@@ -373,12 +372,12 @@ Chooses the optimal quote character to minimize escapes.
 
 Inlines array spread operations with constant arrays.
 
-```ts Input
+```ts title="Input"
 [1, ...[2, 3], 4]
 [...[a, b]]
 ```
 
-```js Output
+```js title="Output"
 [1,2,3,4]
 [a,b]
 ```
@@ -389,13 +388,13 @@ Inlines array spread operations with constant arrays.
 
 Evaluates constant array access at compile time.
 
-```ts Input
+```ts title="Input"
 [x][0]
 ['a', 'b', 'c'][1]
 ['a', , 'c'][1]
 ```
 
-```js Output
+```js title="Output"
 x
 'b'
 void 0
@@ -407,14 +406,14 @@ void 0
 
 Converts bracket notation to dot notation when possible.
 
-```ts Input
+```ts title="Input"
 obj["property"]
 obj["validName"]
 obj["123"]
 obj["invalid-name"]
 ```
 
-```js Output
+```js title="Output"
 obj.property
 obj.validName
 obj["123"]
@@ -427,7 +426,7 @@ obj["invalid-name"]
 
 Evaluates constant comparisons at compile time.
 
-```ts Input
+```ts title="Input"
 3 < 5
 5 > 3
 3 <= 3
@@ -435,7 +434,7 @@ Evaluates constant comparisons at compile time.
 "a" < "b"
 ```
 
-```js Output
+```js title="Output"
 !0
 !0
 !0
@@ -449,14 +448,14 @@ Evaluates constant comparisons at compile time.
 
 Simplifies logical operations with constant values.
 
-```ts Input
+```ts title="Input"
 true && x
 false && x
 true || x
 false || x
 ```
 
-```js Output
+```js title="Output"
 x
 !1
 !0
@@ -469,13 +468,13 @@ x
 
 Evaluates nullish coalescing with known values.
 
-```ts Input
+```ts title="Input"
 null ?? x
 undefined ?? x
 42 ?? x
 ```
 
-```js Output
+```js title="Output"
 x
 x
 42
@@ -487,12 +486,12 @@ x
 
 Removes side-effect-free expressions from comma sequences.
 
-```ts Input
+```ts title="Input"
 (0, x)
 (123, "str", x)
 ```
 
-```js Output
+```js title="Output"
 x
 x
 ```
@@ -503,18 +502,18 @@ x
 
 Evaluates conditional expressions with constant conditions.
 
-```ts Input
+```ts title="Input"
 true ? a : b
 false ? a : b
 x ? true : false
 x ? false : true
 ```
 
-```js Output
+```js title="Output"
 a
 b
-x ? !0 : !1
-x ? !1 : !0
+x
+!x
 ```
 
 ### Unary expression folding
@@ -523,22 +522,19 @@ x ? !1 : !0
 
 Simplifies unary operations.
 
-```ts Input
+```ts title="Input"
 +123
 +"123"
 -(-x)
-~~x
-!!x
+~(~x)
+!(!x)
 ```
 
-```js Output
-123
-123
+```js title="Output"
 123
 123
 x
-~~x
-!!x
+x
 x
 ```
 
@@ -548,12 +544,12 @@ x
 
 Removes unnecessary double negations.
 
-```ts Input
+```ts title="Input"
 !!x
 !!!x
 ```
 
-```js Output
+```js title="Output"
 x
 !x
 ```
@@ -564,14 +560,14 @@ x
 
 Optimizes if statements with constant conditions.
 
-```ts Input
+```ts title="Input"
 if (true) x;
 if (false) x;
 if (x) { a; }
 if (x) {} else y;
 ```
 
-```js Output
+```js title="Output"
 x;
 // removed
 if(x)a;
@@ -584,7 +580,7 @@ if(!x)y;
 
 Removes unreachable code and code without side effects.
 
-```ts Input
+```ts title="Input"
 if (false) {
   unreachable();
 }
@@ -594,7 +590,7 @@ function foo() {
 }
 ```
 
-```js Output
+```js title="Output"
 function foo(){return x}
 ```
 
@@ -604,13 +600,13 @@ function foo(){return x}
 
 Removes branches that can never execute.
 
-```ts Input
+```ts title="Input"
 while (false) {
   neverRuns();
 }
 ```
 
-```js Output
+```js title="Output"
 // removed entirely
 ```
 
@@ -620,13 +616,12 @@ while (false) {
 
 Removes empty blocks and unnecessary braces.
 
-```ts Input
+```ts title="Input"
 { }
 if (x) { }
 ```
 
-```js Output
-;
+```js title="Output"
 // removed
 ```
 
@@ -636,28 +631,27 @@ if (x) { }
 
 Removes unnecessary braces around single statements.
 
-```ts Input
+```ts title="Input"
 if (condition) {
   doSomething();
 }
 ```
 
-```js Output
+```js title="Output"
 if(condition)doSomething();
 ```
 
 ### TypeScript enum inlining
-
 **Mode:** `--minify-syntax`
 
 Inlines TypeScript enum values at compile time.
 
-```ts Input
+```ts title="Input"
 enum Color { Red, Green, Blue }
 const x = Color.Red;
 ```
 
-```js Output
+```js title="Output"
 const x=0;
 ```
 
@@ -667,12 +661,12 @@ const x=0;
 
 Respects `/*@__PURE__*/` annotations for tree shaking.
 
-```ts Input
+```ts title="Input"
 const x = /*@__PURE__*/ expensive();
 // If x is unused...
 ```
 
-```js Output
+```js title="Output"
 // removed entirely
 ```
 
@@ -682,14 +676,14 @@ const x = /*@__PURE__*/ expensive();
 
 Renames local variables to shorter names based on usage frequency.
 
-```ts Input
+```ts title="Input"
 function calculateSum(firstNumber, secondNumber) {
   const result = firstNumber + secondNumber;
   return result;
 }
 ```
 
-```js Output
+```js title="Output"
 function a(b,c){const d=b+c;return d}
 ```
 
@@ -713,14 +707,14 @@ function a(b,c){const d=b+c;return d}
 
 Removes all unnecessary whitespace.
 
-```ts Input
+```ts title="Input"
 function add(a, b) {
-    return a + b;
+  return a + b;
 }
 let x = 10;
 ```
 
-```js Output
+```js title="Output"
 function add(a,b){return a+b;}let x=10;
 ```
 
@@ -730,13 +724,13 @@ function add(a,b){return a+b;}let x=10;
 
 Inserts semicolons only when necessary.
 
-```ts Input
+```ts title="Input"
 let a = 1;
 let b = 2;
 return a + b;
 ```
 
-```js Output
+```js title="Output"
 let a=1;let b=2;return a+b
 ```
 
@@ -746,13 +740,13 @@ let a=1;let b=2;return a+b
 
 Removes spaces around operators.
 
-```ts Input
-a + b
-x = y * z
-foo && bar || baz
+```ts title="Input"
+a + b;
+x = y * z;
+(foo && bar) || baz;
 ```
 
-```js Output
+```js title="Output"
 a+b
 x=y*z
 foo&&bar||baz
@@ -764,33 +758,32 @@ foo&&bar||baz
 
 Removes comments except important license comments.
 
-```ts Input
+```ts title="Input"
 // This comment is removed
 /* So is this */
 /*! But this license comment is kept */
 function test() { /* inline comment */ }
 ```
 
-```js Output
+```js title="Output"
 /*! But this license comment is kept */
 function test(){}
 ```
 
 ### Object and array formatting
-
 **Mode:** `--minify-whitespace`
 
 Removes whitespace in object and array literals.
 
-```ts Input
+```ts title="Input"
 const obj = {
-    name: "John",
-    age: 30
+  name: "John",
+  age: 30,
 };
 const arr = [1, 2, 3];
 ```
 
-```js Output
+```js title="Output"
 const obj={name:"John",age:30};const arr=[1,2,3];
 ```
 
@@ -800,16 +793,16 @@ const obj={name:"John",age:30};const arr=[1,2,3];
 
 Removes whitespace in control structures.
 
-```ts Input
+```ts title="Input"
 if (condition) {
-    doSomething();
+  doSomething();
 }
 for (let i = 0; i < 10; i++) {
-    console.log(i);
+  console.log(i);
 }
 ```
 
-```js Output
+```js title="Output"
 if(condition)doSomething();for(let i=0;i<10;i++)console.log(i);
 ```
 
@@ -819,14 +812,14 @@ if(condition)doSomething();for(let i=0;i<10;i++)console.log(i);
 
 Removes whitespace in function declarations.
 
-```ts Input
+```ts title="Input"
 function myFunction(param1, param2) {
-    return param1 + param2;
+  return param1 + param2;
 }
 const arrow = (a, b) => a + b;
 ```
 
-```js Output
+```js title="Output"
 function myFunction(a,b){return a+b}const arrow=(a,b)=>a+b;
 ```
 
@@ -836,13 +829,13 @@ function myFunction(a,b){return a+b}const arrow=(a,b)=>a+b;
 
 Only adds parentheses when necessary for operator precedence.
 
-```ts Input
+```ts title="Input"
 (a + b) * c
 a + (b * c)
 ((x))
 ```
 
-```js Output
+```js title="Output"
 (a+b)*c
 a+b*c
 x
@@ -854,11 +847,11 @@ x
 
 Renames object properties to shorter names when configured.
 
-```ts Input
+```ts title="Input"
 obj.longPropertyName
 ```
 
-```js Output (with property mangling enabled)
+```js title="Output (with property mangling enabled)"
 obj.a
 ```
 
@@ -868,7 +861,7 @@ obj.a
 
 Converts non-string interpolated values to strings and folds them into the template.
 
-```ts Input
+```ts title="Input"
 `hello ${123}`
 `value: ${true}`
 `result: ${null}`
@@ -876,7 +869,7 @@ Converts non-string interpolated values to strings and folds them into the templ
 `big: ${10n}`
 ```
 
-```js Output
+```js title="Output"
 "hello 123"
 "value: true"
 "result: null"
@@ -890,12 +883,12 @@ Converts non-string interpolated values to strings and folds them into the templ
 
 Evaluates `.length` property on string literals at compile time.
 
-```ts Input
+```ts title="Input"
 "hello world".length
 "test".length
 ```
 
-```js Output
+```js title="Output"
 11
 4
 ```
@@ -906,7 +899,7 @@ Evaluates `.length` property on string literals at compile time.
 
 Simplifies constructor calls for built-in types.
 
-```ts Input
+```ts title="Input"
 new Object()
 new Object(null)
 new Object({a: 1})
@@ -914,7 +907,7 @@ new Array()
 new Array(x, y)
 ```
 
-```js Output
+```js title="Output"
 {}
 {}
 {a:1}
@@ -928,11 +921,11 @@ new Array(x, y)
 
 Inlines property access for objects with a single property.
 
-```ts Input
+```ts title="Input"
 ({fn: () => console.log('hi')}).fn()
 ```
 
-```js Output
+```js title="Output"
 (() => console.log('hi'))()
 ```
 
@@ -942,12 +935,12 @@ Inlines property access for objects with a single property.
 
 Evaluates `charCodeAt()` on string literals for ASCII characters.
 
-```ts Input
+```ts title="Input"
 "hello".charCodeAt(1)
 "A".charCodeAt(0)
 ```
 
-```js Output
+```js title="Output"
 101
 65
 ```
@@ -958,12 +951,12 @@ Evaluates `charCodeAt()` on string literals for ASCII characters.
 
 Converts loose equality checks with `void 0` to `null` since they're equivalent.
 
-```ts Input
+```ts title="Input"
 x == void 0
 x != void 0
 ```
 
-```js Output
+```js title="Output"
 x == null
 x != null
 ```
@@ -974,12 +967,12 @@ x != null
 
 Moves negation operator through comma expressions.
 
-```ts Input
+```ts title="Input"
 -(a, b)
 -(x, y, z)
 ```
 
-```js Output
+```js title="Output"
 a,-b
 x,y,-z
 ```
@@ -990,18 +983,18 @@ x,y,-z
 
 Inlines `import.meta` properties at build time when values are known.
 
-```ts Input
-import.meta.dir
-import.meta.file
-import.meta.path
-import.meta.url
+```ts title="Input"
+import.meta.dir;
+import.meta.file;
+import.meta.path;
+import.meta.url;
 ```
 
-```js Output
-"/path/to/directory"
-"filename.js"
-"/full/path/to/file.js"
-"file:///full/path/to/file.js"
+```js title="Output"
+"/path/to/directory";
+"filename.js";
+"/full/path/to/file.js";
+"file:///full/path/to/file.js";
 ```
 
 ### Variable declaration merging
@@ -1010,14 +1003,14 @@ import.meta.url
 
 Merges adjacent variable declarations of the same type.
 
-```ts Input
+```ts title="Input"
 let a = 1;
 let b = 2;
 const c = 3;
 const d = 4;
 ```
 
-```js Output
+```js title="Output"
 let a=1,b=2;
 const c=3,d=4;
 ```
@@ -1028,13 +1021,13 @@ const c=3,d=4;
 
 Merges adjacent expression statements using comma operator.
 
-```ts Input
+```ts title="Input"
 console.log(1);
 console.log(2);
 console.log(3);
 ```
 
-```js Output
+```js title="Output"
 console.log(1),console.log(2),console.log(3);
 ```
 
@@ -1044,12 +1037,12 @@ console.log(1),console.log(2),console.log(3);
 
 Merges expressions before return with comma operator.
 
-```ts Input
+```ts title="Input"
 console.log(x);
 return y;
 ```
 
-```js Output
+```js title="Output"
 return console.log(x),y;
 ```
 
@@ -1059,12 +1052,12 @@ return console.log(x),y;
 
 Merges expressions before throw with comma operator.
 
-```ts Input
+```ts title="Input"
 console.log(x);
 throw new Error();
 ```
 
-```js Output
+```js title="Output"
 throw(console.log(x),new Error());
 ```
 
@@ -1074,7 +1067,7 @@ throw(console.log(x),new Error());
 
 Inlines enum values across module boundaries.
 
-```ts Input 
+```ts title="Input"
 // lib.ts
 export enum Color { Red, Green, Blue }
 
@@ -1083,22 +1076,21 @@ import { Color } from './lib';
 const x = Color.Red;
 ```
 
-```js Output
+```js title="Output"
 const x=0;
 ```
 
 ### Computed property enum inlining
-
 **Mode:** `--minify-syntax`
 
 Inlines enum values used as computed object properties.
 
-```ts Input
+```ts title="Input"
 enum Keys { FOO = 'foo' }
 const obj = { [Keys.FOO]: value }
 ```
 
-```js Output
+```js title="Output"
 const obj={foo:value}
 ```
 
@@ -1108,12 +1100,12 @@ const obj={foo:value}
 
 Converts string numeric property access to numeric index.
 
-```ts Input
+```ts title="Input"
 obj["0"]
 arr["5"]
 ```
 
-```js Output
+```js title="Output"
 obj[0]
 arr[5]
 ```
@@ -1124,12 +1116,12 @@ arr[5]
 
 Uses expression body syntax when an arrow function only returns a value.
 
-```ts Input
+```ts title="Input"
 () => { return x; }
 (a) => { return a + 1; }
 ```
 
-```js Output
+```js title="Output"
 () => x
 a => a + 1
 ```
@@ -1140,12 +1132,12 @@ a => a + 1
 
 Uses shorthand syntax when property name and value identifier match.
 
-```ts Input
+```ts title="Input"
 { x: x, y: y }
 { name: name, age: age }
 ```
 
-```js Output
+```js title="Output"
 { x, y }
 { name, age }
 ```
@@ -1156,14 +1148,14 @@ Uses shorthand syntax when property name and value identifier match.
 
 Uses method shorthand syntax in object literals.
 
-```ts Input
+```ts title="Input"
 {
   foo: function() {},
   bar: async function() {}
 }
 ```
 
-```js Output
+```js title="Output"
 {
   foo() {},
   async bar() {}
@@ -1176,14 +1168,14 @@ Uses method shorthand syntax in object literals.
 
 Removes `debugger` statements from code.
 
-```ts Input
+```ts title="Input"
 function test() {
   debugger;
   return x;
 }
 ```
 
-```js Output
+```js title="Output"
 function test(){return x}
 ```
 
@@ -1193,13 +1185,13 @@ function test(){return x}
 
 Removes all `console.*` method calls from code.
 
-```ts Input
+```ts title="Input"
 console.log("debug");
 console.warn("warning");
 x = console.error("error");
 ```
 
-```js Output
+```js title="Output"
 void 0;
 void 0;
 x=void 0;
@@ -1211,12 +1203,12 @@ x=void 0;
 
 Removes calls to specified global functions or methods.
 
-```ts Input
+```ts title="Input"
 assert(condition);
 obj.assert(test);
 ```
 
-```js Output with --drop=assert
+```js title="Output with --drop=assert"
 void 0;
 void 0;
 ```
@@ -1248,7 +1240,7 @@ This preserves the `.name` property on functions and classes while still minifyi
 
 Using all three minification modes together:
 
-```ts input.ts (158 bytes)
+```ts title="input.ts (158 bytes)"
 const myVariable = 42;
 
 const myFunction = () => {
diff --git b/content/docs/bundler/plugins.mdx a/content/docs/bundler/plugins.mdx
index b85954ab..a24e1dcf 100644
--- b/content/docs/bundler/plugins.mdx
+++ a/content/docs/bundler/plugins.mdx
@@ -341,7 +341,7 @@ To create a native plugin, you must export a C ABI function which matches the si
 
 ```bash title="terminal"
 $ bun add -g @napi-rs/cli
-napi new
+$ napi new
 ```
 
 Then install this crate:
diff --git b/content/docs/feedback.mdx a/content/docs/feedback.mdx
index ff966d5a..7bee792d 100644
--- b/content/docs/feedback.mdx
+++ a/content/docs/feedback.mdx
@@ -2,11 +2,12 @@
 title: Feedback
 description: Share feedback, bug reports, and feature requests
 mode: center
+icon: Lightbulb
 ---
 
 Whether you've found a bug, have a performance issue, or just want to suggest an improvement, here's how you can open a helpful issue:
 
-<Callout icon="discord">For general questions, please join our [Discord](https://bun.com/discord).</Callout>
+<Callout>For general questions, please join our [Discord](https://bun.com/discord).</Callout>
 
 ## Reporting Issues
 
@@ -57,7 +58,7 @@ Whether you've found a bug, have a performance issue, or just want to suggest an
     	- The operating system and version you are using
     		<Callout type="info">
     			- For MacOS and Linux: copy the output of `uname -mprs`
-    		  - For Windows: copy the output of this command in the powershell console:
+    		  	- For Windows: copy the output of this command in the powershell console:
     				`"$([Environment]::OSVersion | ForEach-Object VersionString) $(if ([Environment]::Is64BitOperatingSystem) { "x64" } else { "x86" })"`
     		</Callout>
     </Step>
diff --git b/content/docs/index.mdx a/content/docs/index.mdx
index f97d8e33..82de5b7a 100644
--- b/content/docs/index.mdx
+++ a/content/docs/index.mdx
@@ -4,73 +4,68 @@ description: Bun is an all-in-one toolkit for developing modern JavaScript/TypeS
 mode: center
 ---
 
-<Cards>
-  <Card
-    icon="cog"
-    title="Bun Runtime"
-    href="/runtime"
-    cta={
-      <span>
-        Get started with <code>bun run</code>
-      </span>
-    }
-  >
-    A fast JavaScript runtime designed as a drop-in replacement for Node.js
-  </Card>
-  <Card
-    icon="box"
-    title="Bun Package Manager"
-    href="/pm/cli/install"
-    cta={
-      <span>
-        Get started with <code>bun install</code>
-      </span>
-    }
-  >
-    Install packages up to 30x faster than npm with a global cache and workspaces
-  </Card>
-  <Card
-    icon="flask-conical"
-    title="Bun Test Runner"
-    href="/test"
-    cta={
-      <span>
-        Get started with <code>bun test</code>
-      </span>
-    }
-  >
-    Jest-compatible, TypeScript-first tests with snapshots, DOM, and watch mode
-  </Card>
-  <Card
-    icon="combine"
-    title="Bun Bundler"
-    href="/bundler"
-    cta={
-      <span>
-        Get started with <code>bun build</code>
-      </span>
-    }
-  >
-    Bundle TypeScript, JSX, React & CSS for both browsers and servers
-  </Card>
-</Cards>
+{/* start llm ignored codeblock */}
+import { CogIcon, BoxIcon, FlaskConicalIcon, CombineIcon, ChevronRightIcon } from "lucide-react";
+import { Card as FancyCard, Cards as FancyCards } from "@/components/ui/card";
+
+<FancyCards>
+	<FancyCard	
+		icon={<CogIcon />}
+		title="Bun Runtime"
+		href="/docs/runtime"
+        cta={<span>Get started with <code>bun run</code></span>}
+	>
+		A fast JavaScript runtime designed as a drop-in replacement for Node.js
+    </FancyCard>
+    <FancyCard
+    	icon={<BoxIcon />}
+    	title="Bun Package Manager"
+    	href="/docs/pm/cli/install"
+    	cta={<span>Get started with <code>bun install</code></span>}
+    >
+    	Install packages up to 30x faster than npm with a global cache and workspaces
+    </FancyCard>
+    <FancyCard
+    	icon={<FlaskConicalIcon />}
+    	title="Bun Test Runner"
+    	href="/docs/test"
+    	cta={<span>Get started with <code>bun test</code></span>}
+    >
+    	Jest-compatible, TypeScript-first tests with snapshots, DOM, and watch mode
+    </FancyCard>
+    <FancyCard
+    	icon={<CombineIcon />}
+    	title="Bun Bundler"
+    	href="/docs/bundler"
+    	cta={<span>Get started with <code>bun build</code></span>}
+    >
+    	Bundle TypeScript, JSX, React & CSS for both browsers and servers
+    </FancyCard>
+</FancyCards>
 
 ---
+{/* end llm ignored codeblock */}
 
 ## Get Started
 
 Bun ships as a single, dependency-free binary and includes a runtime, package manager, test runner, and bundler. New to Bun?
 
-<Cards>
-  <Card icon="download" title="Install Bun" href="/installation">
+{/* start llm ignored codeblock */}
+import { DownloadIcon, ZapIcon, LightbulbIcon } from 'lucide-react';
+
+<Cards className="grid-cols-3">
+  <Card icon={<DownloadIcon />} title="Install Bun" href="/docs/installation">
     Supported platforms and all install methods.
   </Card>
-  <Card icon="zap" title="Quickstart" href="/quickstart">
+  <Card icon={<ZapIcon />} title="Quickstart" href="/docs/quickstart">
     Hello world in minutes with Bun.serve.
   </Card>
-</Cards>
-
+    <Card icon={<LightbulbIcon />} title="Feedback" href="/feedback">
+    Share feedback, bug reports, and feature requests.
+  </Card>
+</Cards> 
 ---
+{/* end llm ignored codeblock */}
 
 ## What's Inside
 
diff --git b/content/docs/installation.mdx a/content/docs/installation.mdx
index e36cbf98..7d705669 100644
--- b/content/docs/installation.mdx
+++ a/content/docs/installation.mdx
@@ -13,13 +13,9 @@ Bun ships as a single, dependency-free executable. You can install it via script
 
 <Tabs items={['macOS & Linux', 'Windows', 'Package Managers', 'Docker']}>
   <Tab value="macOS & Linux">
-
-    
-
-```bash curl
-$ curl -fsSL https://bun.com/install | bash
-```
-
+      ```bash title="curl"
+      $ curl -fsSL https://bun.com/install | bash
+      ```
     <Callout type="info">
       **Linux users**  The `unzip` package is required to install Bun. Use `sudo apt install unzip` to install the unzip package. Kernel version 5.6 or higher is strongly recommended, but the minimum is 5.1. Use `uname -r` to check Kernel version.
       </Callout>
@@ -27,12 +23,9 @@ $ curl -fsSL https://bun.com/install | bash
   </Tab>
 
   <Tab value="Windows">
-    
-
-```powershell PowerShell
-> powershell -c "irm bun.sh/install.ps1|iex"
-```
-
+      ```powershell title="PowerShell"
+      > powershell -c "irm bun.sh/install.ps1|iex"
+      ```
     <Callout type="warning">
       Bun requires Windows 10 version 1809 or later.
     </Callout>
@@ -40,22 +33,16 @@ $ curl -fsSL https://bun.com/install | bash
     For support and discussion, please join the **#windows** channel on our [Discord](https://bun.com/discord).
 
   </Tab>
-
     <Tab value="Package Managers">
-    	
-
-```bash tab="npm"
-$ npm install -g bun # the last `npm` command you'll ever need
-```
-
-```bash tab="Homebrew"
-$ brew install oven-sh/bun/bun
-```
-
-```bash tab="Scoop"
-$ scoop install bun
-```
-
+    	```bash title="npm"
+      $ npm install -g bun # the last `npm` command you'll ever need
+      ```
+      ```bash title="Homebrew"
+      $ brew install oven-sh/bun/bun
+      ```
+    	```bash title="Scoop"
+    	$ scoop install bun
+      ```
     </Tab>
 
   <Tab value="Docker">
@@ -64,7 +51,7 @@ $ scoop install bun
     ```bash title="Docker"
     $ docker pull oven/bun
     $ docker run --rm --init --ulimit memlock=-1:-1 oven/bun
-   ```
+    ```
 
     ### Image Variants
 
@@ -75,8 +62,7 @@ $ scoop install bun
     $ docker pull oven/bun:slim
     $ docker pull oven/bun:distroless
     $ docker pull oven/bun:alpine
-   ```
-
+    ```
   </Tab>
 </Tabs>
 
@@ -84,96 +70,93 @@ To check that Bun was installed successfully, open a new terminal window and run
 
 ```bash title="terminal"
 $ bun --version
-Output: 1.x.y
+1.x.y
 
 # See the precise commit of `oven-sh/bun` that you're using
 $ bun --revision
-Output: 1.x.y+b7982ac13189
+1.x.y+b7982ac13189
 ```
 
 <Callout type="warning">
-  If you've installed Bun but are seeing a `command not found` error, you may have to manually add the installation
-  directory (`~/.bun/bin`) to your `PATH`.
+	If you've installed Bun but are seeing a `command not found` error, you may have to manually add
+	the installation directory (`~/.bun/bin`) to your `PATH`.
 </Callout>
 
-<Accordions type="single">
+<Accordions>
 <Accordion title="Add Bun to your PATH">
   <Tabs items={['macOS & Linux', 'Windows']}>
     <Tab value="macOS & Linux">
       <Steps>
         <Step>
-
           ### Determine which shell you're using
-        ```bash title="terminal"
-        $ echo $SHELL
-        # /bin/zsh  or /bin/bash or /bin/fish
-   ```
+
+          ```bash title="terminal"
+          $ echo $SHELL
+          # /bin/zsh  or /bin/bash or /bin/fish
+          ```
         </Step>
         <Step>
-
           ### Open your shell configuration file
+
           - For bash: `~/.bashrc`
           - For zsh: `~/.zshrc`
           - For fish: `~/.config/fish/config.fish`
         </Step>
         
         <Step>
-
-        
           ### Add the Bun directory to PATH
+
           Add this line to your configuration file:
           ```bash title="terminal"
           $ export BUN_INSTALL="$HOME/.bun"
           $ export PATH="$BUN_INSTALL/bin:$PATH"
-   ```
+          ```
         </Step>
         
         <Step>
-
-        
           ### Reload your shell configuration
+
           ```bash title="terminal"
-          $ source ~/.bashrc  # or ~/.zshrc
-   ```
+          $ source ~/.bashrc # or ~/.zshrc
+          ```
         </Step>
       </Steps>
     </Tab>
     <Tab value="Windows">
       <Steps>
         <Step>
-
           ### Determine if the bun binary is properly installed
-        ```bash title="terminal"
-        & "$env:USERPROFILE\.bun\bin\bun" --version
-   ```
 
-        If the command runs successfully but `bun --version` is not recognized, it means that bun is not in your system's PATH. To fix this, open a Powershell terminal and run the following command:
+          ```bash title="terminal"
+          $ & "$env:USERPROFILE\.bun\bin\bun" --version
+          ```
 
-        ```bash title="terminal"
-        [System.Environment]::SetEnvironmentVariable(
-          "Path",
-          [System.Environment]::GetEnvironmentVariable("Path", "User") + ";$env:USERPROFILE\.bun\bin",
-          [System.EnvironmentVariableTarget]::User
-        )
-   ```
+          If the command runs successfully but `bun --version` is not recognized, it means that bun is not in your system's PATH. To fix this, open a Powershell terminal and run the following command:
 
+          ```bash title="terminal"
+          [System.Environment]::SetEnvironmentVariable(
+            "Path",
+            [System.Environment]::GetEnvironmentVariable("Path", "User") + ";$env:USERPROFILE\.bun\bin",
+            [System.EnvironmentVariableTarget]::User
+          )
+          ```
         </Step>
         <Step>
-
           ### Restart your terminal
-           After running the command, restart your terminal and test with `bun --version`
+
+          After running the command, restart your terminal and test with `bun --version`
 
           ```bash title="terminal"
           $ bun --version
-   ```
+          ```
         </Step>
       </Steps>
     </Tab>
 
   </Tabs>
 </Accordion>
-</Accordions>
 
+</Accordions>
 ---
 
 ## Upgrading
@@ -223,16 +206,14 @@ Since Bun is a single binary, you can install older versions by re-running the i
 
     ```bash title="terminal"
     $ curl -fsSL https://bun.com/install | bash -s "bun-v$BUN_LATEST_VERSION"
-   ```
-
+    ```
   </Tab>
   <Tab value="Windows">
     On Windows, pass the version number to the PowerShell install script:
 
     ```powershell title="PowerShell"
     > iex "& {$(irm https://bun.com/install.ps1)} -Version $BUN_LATEST_VERSION"
-   ```
-
+    ```
   </Tab>
 </Tabs>
 
@@ -245,62 +226,62 @@ To download Bun binaries directly, visit the [releases page on GitHub](https://g
 ### Latest Version Downloads
 
 <Cards cols={2}>
-  <Card
-    icon="/icons/linux.svg"
-    title="Linux x64"
-    href="https://github.com/oven-sh/bun/releases/latest/download/bun-linux-x64.zip"
-  >
-    Standard Linux x64 binary
-  </Card>
-  <Card
-    icon="/icons/linux.svg"
-    title="Linux x64 Baseline"
-    href="https://github.com/oven-sh/bun/releases/latest/download/bun-linux-x64-baseline.zip"
-  >
-    For older CPUs without AVX2
-  </Card>
-  <Card
-    icon="/icons/windows.svg"
-    title="Windows x64"
-    href="https://github.com/oven-sh/bun/releases/latest/download/bun-windows-x64.zip"
-  >
-    Standard Windows binary
-  </Card>
-  <Card
-    icon="/icons/windows.svg"
-    title="Windows x64 Baseline"
-    href="https://github.com/oven-sh/bun/releases/latest/download/bun-windows-x64-baseline.zip"
-  >
-    For older CPUs without AVX2
-  </Card>
-  <Card
-    icon="/icons/windows.svg"
+	<Card
+	
+		title="Linux x64"
+		href="https://github.com/oven-sh/bun/releases/latest/download/bun-linux-x64.zip"
+	>
+		Standard Linux x64 binary
+	</Card>
+	<Card
+	
+		title="Linux x64 Baseline"
+		href="https://github.com/oven-sh/bun/releases/latest/download/bun-linux-x64-baseline.zip"
+	>
+		For older CPUs without AVX2
+	</Card>
+	<Card
+	
+		title="Windows x64"
+		href="https://github.com/oven-sh/bun/releases/latest/download/bun-windows-x64.zip"
+	>
+		Standard Windows binary
+	</Card>
+	<Card
+
+		title="Windows x64 Baseline"
+		href="https://github.com/oven-sh/bun/releases/latest/download/bun-windows-x64-baseline.zip"
+	>
+		For older CPUs without AVX2
+	</Card>
+	<Card
+
     title="Windows ARM64"
     href="https://github.com/oven-sh/bun/releases/latest/download/bun-windows-aarch64.zip"
   >
     Windows on ARM (Snapdragon, etc.)
   </Card>
   <Card
-    icon="/icons/apple.svg"
-    title="macOS ARM64"
-    href="https://github.com/oven-sh/bun/releases/latest/download/bun-darwin-aarch64.zip"
-  >
-    Apple Silicon (M1/M2/M3)
-  </Card>
-  <Card
-    icon="/icons/apple.svg"
-    title="macOS x64"
-    href="https://github.com/oven-sh/bun/releases/latest/download/bun-darwin-x64.zip"
-  >
-    Intel Macs
-  </Card>
-  <Card
-    icon="/icons/linux.svg"
-    title="Linux ARM64"
-    href="https://github.com/oven-sh/bun/releases/latest/download/bun-linux-aarch64.zip"
-  >
-    ARM64 Linux systems
-  </Card>
+
+		title="macOS ARM64"
+		href="https://github.com/oven-sh/bun/releases/latest/download/bun-darwin-aarch64.zip"
+	>
+		Apple Silicon (M1/M2/M3)
+	</Card>
+	<Card
+	
+		title="macOS x64"
+		href="https://github.com/oven-sh/bun/releases/latest/download/bun-darwin-x64.zip"
+	>
+		Intel Macs
+	</Card>
+	<Card
+	
+		title="Linux ARM64"
+		href="https://github.com/oven-sh/bun/releases/latest/download/bun-linux-aarch64.zip"
+	>
+		ARM64 Linux systems
+	</Card>
 </Cards>
 
 ### Musl Binaries
@@ -312,8 +293,9 @@ For distributions without `glibc` (Alpine Linux, Void Linux):
 - [Linux ARM64 musl](https://github.com/oven-sh/bun/releases/latest/download/bun-linux-aarch64-musl.zip)
 
 <Callout type="info">
-  If you encounter an error like `bun: /lib/x86_64-linux-gnu/libm.so.6: version GLIBC_2.29 not found`, try using the
-  musl binary. Bun's install script automatically chooses the correct binary for your system.
+	If you encounter an error like `bun: /lib/x86_64-linux-gnu/libm.so.6: version GLIBC_2.29 not
+	found`, try using the musl binary. Bun's install script automatically chooses the correct binary
+	for your system.
 </Callout>
 
 ---
@@ -330,23 +312,22 @@ Bun has specific CPU requirements based on the binary you're using:
     | x64 | Haswell (4th gen Core) or newer | Excavator or newer |
 	</Tab>
 
-    <Tab value="Baseline Builds">
-    	**x64-baseline binaries** target the Nehalem architecture for older CPUs
+	<Tab value="Baseline Builds">
+		**x64-baseline binaries** target the Nehalem architecture for older CPUs
     | Platform | Intel Requirement | AMD Requirement |
-    |----------|-------------------|-----------------|
+    |----------|-------------------|-----------------| 
     | x64-baseline | Nehalem (1st gen Core) or newer | Bulldozer or newer |
 
-    	<Callout type="warning">
-    		Baseline builds are slower than regular builds. Use them only if you encounter an "Illegal
-    		Instruction" error.
-    	</Callout>
-    </Tab>
-
+		<Callout type="warning">
+			Baseline builds are slower than regular builds. Use them only if you encounter an "Illegal
+			Instruction" error.
+		</Callout>
+	</Tab>
 </Tabs>
 
 <Callout type="info">
-  Bun does not support CPUs older than the baseline target, which mandates the SSE4.2 extension. macOS requires version
-  13.0 or later.
+	Bun does not support CPUs older than the baseline target, which mandates the SSE4.2 extension.
+	macOS requires version 13.0 or later.
 </Callout>
 
 ---
@@ -357,32 +338,26 @@ To remove Bun from your system:
 
 <Tabs items={['macOS & Linux', 'Windows', 'Package Managers']}>
 	<Tab value="macOS & Linux">
-  ```bash title="terminal"
-  $ rm -rf ~/.bun
-  ```
+    ```bash title="terminal"
+    $ rm -rf ~/.bun
+    ```
   </Tab>
 
-    <Tab value="Windows">
-    	```powershell title="PowerShell"
+	<Tab value="Windows">
+		```powershell title="PowerShell"
     > powershell -c ~\.bun\uninstall.ps1
-   ```
-    </Tab>
-
-    <Tab value="Package Managers">
-    	
-
-```bash tab="npm"
-$ npm uninstall -g bun
-```
-
-```bash tab="Homebrew"
-$ brew uninstall bun
-```
-
-```bash tab="Scoop"
-$ scoop uninstall bun
-```
-
-    </Tab>
+    ```
+	</Tab>
 
+	<Tab value="Package Managers">
+			```bash title="npm"
+      $ npm uninstall -g bun
+      ```
+      ```bash title="Homebrew"
+      $ brew uninstall bun
+      ```
+      ```bash title="Scoop"
+      $ scoop uninstall bun
+      ```
+	</Tab>
 </Tabs>
diff --git b/content/docs/pm/bunx.mdx a/content/docs/pm/bunx.mdx
index e3556aba..f79aea1b 100644
--- b/content/docs/pm/bunx.mdx
+++ a/content/docs/pm/bunx.mdx
@@ -52,8 +52,6 @@ To pass additional command-line flags and arguments through to the executable, p
 $ bunx my-cli --foo bar
 ```
 
----
-
 ## Shebangs
 
 By default, Bun respects shebangs. If an executable is marked with `#!/usr/bin/env node`, Bun will spin up a `node` process to execute the file. However, in some cases it may be desirable to run executables using Bun's runtime, even if the executable indicates otherwise. To do so, include the `--bun` flag.
@@ -83,7 +81,6 @@ To force bun to always be used with a script, use a shebang.
 ```js title="dist/index.js"
 #!/usr/bin/env bun
 ```
-
 ---
 
-<Bunx />
+<include>../../_snippets/cli/bunx.mdx</include>
diff --git b/content/docs/pm/cli/link.mdx a/content/docs/pm/cli/link.mdx
index c29e1378..49fd90bd 100644
--- b/content/docs/pm/cli/link.mdx
+++ a/content/docs/pm/cli/link.mdx
@@ -7,7 +7,7 @@ Use `bun link` in a local directory to register the current package as a "linkab
 
 ```bash title="terminal"
 $ cd /path/to/cool-pkg
-cat package.json
+$ cat package.json
 $ bun link
 
 bun link v$BUN_LATEST_VERSION (7416672e)
diff --git b/content/docs/pm/cli/outdated.mdx a/content/docs/pm/cli/outdated.mdx
index 7bd9cc5c..03996471 100644
--- b/content/docs/pm/cli/outdated.mdx
+++ a/content/docs/pm/cli/outdated.mdx
@@ -19,7 +19,6 @@ $ bun outdated
 | prettier (dev)                 | 3.4.2   | 3.5.0     | 3.5.0      |
 | tsup (dev)                     | 8.3.5   | 8.3.6     | 8.3.6      |
 | typescript (dev)               | 5.7.2   | 5.7.3     | 5.7.3      |
-
 ```
 
 ## Version Information
@@ -43,7 +42,6 @@ $ bun outdated eslint-plugin-security eslint-plugin-sonarjs
 | ------------------------------ | ------- | ------ | --------- |
 | eslint-plugin-security (dev)   | 2.1.1   | 2.1.1  | 3.0.1     |
 | eslint-plugin-sonarjs (dev)    | 0.23.0  | 0.23.0 | 3.0.1     |
-
 ```
 
 You can also pass glob patterns to check for outdated packages:
@@ -65,7 +63,7 @@ $ bun outdated '@types/*'
 
 | Package            | Current | Update | Latest |
 | ------------------ | ------- | ------ | ------ |
-| @types/bun (dev)   | 1.3.0   | 1.3.3  | 1.3.3 |
+| @types/bun (dev)   | 1.3.0   | 1.3.3  | 1.3.3  |
 ```
 
 Or to exclude all `@types/*` packages:
diff --git b/content/docs/pm/cli/pm.mdx a/content/docs/pm/cli/pm.mdx
index 2ef06140..99c220a7 100644
--- b/content/docs/pm/cli/pm.mdx
+++ a/content/docs/pm/cli/pm.mdx
@@ -87,7 +87,6 @@ To print the path to the `bin` directory for the local project:
 
 ```bash title="terminal"
 $ bun pm bin
-
 /path/to/current/project/node_modules/.bin
 ```
 
@@ -95,7 +94,6 @@ To print the path to the global `bin` directory:
 
 ```bash title="terminal"
 $ bun pm bin -g
-
 <$HOME>/.bun/bin
 ```
 
diff --git b/content/docs/pm/cli/publish.mdx a/content/docs/pm/cli/publish.mdx
index 8ab4e2b8..a1fc945e 100644
--- b/content/docs/pm/cli/publish.mdx
+++ a/content/docs/pm/cli/publish.mdx
@@ -9,6 +9,7 @@ description: Use `bun publish` to publish a package to the npm registry
 ## Publishing the package from the current working directory
 $ bun publish
 
+## Output
 bun publish v$BUN_LATEST_VERSION (ca7428e9)
 
 packed 203B package.json
diff --git b/content/docs/pm/cli/update.mdx a/content/docs/pm/cli/update.mdx
index 9bcc6ffd..98faa4c8 100644
--- b/content/docs/pm/cli/update.mdx
+++ a/content/docs/pm/cli/update.mdx
@@ -32,7 +32,9 @@ This launches an interactive terminal interface that shows all outdated packages
 
 The interface displays packages grouped by dependency type:
 
-```txt
+```sh title="terminal"
+$ bun update -i
+
 ? Select packages to update - Space to toggle, Enter to confirm, a to select all, n to select none, i to invert, l to toggle latest
 
   dependencies                Current  Target   Latest
diff --git b/content/docs/pm/npmrc.mdx a/content/docs/pm/npmrc.mdx
index 5bc2a4cc..191b0b02 100644
--- b/content/docs/pm/npmrc.mdx
+++ a/content/docs/pm/npmrc.mdx
@@ -1,6 +1,6 @@
 ---
 title: .npmrc support
-description:
+description: "Package Manager npmrc configuration"
 ---
 
 Bun supports loading configuration options from [`.npmrc`](https://docs.npmjs.com/cli/v10/configuring-npm/npmrc) files, allowing you to reuse existing registry/scope configurations.
diff --git b/content/docs/pm/security-scanner-api.mdx a/content/docs/pm/security-scanner-api.mdx
index 4509e5b9..5e448032 100644
--- b/content/docs/pm/security-scanner-api.mdx
+++ a/content/docs/pm/security-scanner-api.mdx
@@ -1,6 +1,5 @@
 ---
 title: Security Scanner API
-description:
 ---
 
 Bun's package manager can scan packages for security vulnerabilities before installation, helping protect your applications from supply chain attacks and known vulnerabilities.
diff --git b/content/docs/project/building-windows.mdx a/content/docs/project/building-windows.mdx
index 53b22b91..71f9732b 100644
--- b/content/docs/project/building-windows.mdx
+++ a/content/docs/project/building-windows.mdx
@@ -14,7 +14,7 @@ It is strongly recommended to use [PowerShell 7 (`pwsh.exe`)](https://learn.micr
 By default, running unverified scripts are blocked.
 
 ```ps1
-Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy Unrestricted
+> Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy Unrestricted
 ```
 
 ### System Dependencies
@@ -22,7 +22,7 @@ Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy Unrestricted
 Bun v1.1 or later. We use Bun to run it's own code generators.
 
 ```ps1
-irm bun.sh/install.ps1 | iex
+> irm bun.sh/install.ps1 | iex
 ```
 
 [Visual Studio](https://visualstudio.microsoft.com) with the "Desktop Development with C++" workload. While installing, make sure to install Git as well, if Git for Windows is not already installed.
@@ -30,7 +30,7 @@ irm bun.sh/install.ps1 | iex
 Visual Studio can be installed graphically using the wizard or through WinGet:
 
 ```ps1
-winget install "Visual Studio Community 2022" --override "--add Microsoft.VisualStudio.Workload.NativeDesktop Microsoft.VisualStudio.Component.Git " -s msstore
+> winget install "Visual Studio Community 2022" --override "--add Microsoft.VisualStudio.Workload.NativeDesktop Microsoft.VisualStudio.Component.Git " -s msstore
 ```
 
 After Visual Studio, you need the following:
@@ -47,19 +47,19 @@ After Visual Studio, you need the following:
 
 Use [Scoop](https://scoop.sh) to install these remaining tools.
 
-```ps1 Scoop (x64)
-irm https://get.scoop.sh | iex
-scoop install nodejs-lts go rust nasm ruby perl ccache
+```ps1 title="Scoop (x64)"
+> irm https://get.scoop.sh | iex
+> scoop install nodejs-lts go rust nasm ruby perl ccache
 # scoop seems to be buggy if you install llvm and the rest at the same time
-scoop install llvm@21.1.8
+> scoop install llvm@21.1.8
 ```
 
 For Windows ARM64, download LLVM 21.1.8 directly from GitHub releases (first version with ARM64 Windows builds):
 
-```ps1 ARM64
+```ps1 title="ARM64"
 # Download and install LLVM for ARM64
-Invoke-WebRequest -Uri "https://github.com/llvm/llvm-project/releases/download/llvmorg-21.1.8/LLVM-21.1.8-woa64.exe" -OutFile "$env:TEMP\LLVM-21.1.8-woa64.exe"
-Start-Process -FilePath "$env:TEMP\LLVM-21.1.8-woa64.exe" -ArgumentList "/S" -Wait
+> Invoke-WebRequest -Uri "https://github.com/llvm/llvm-project/releases/download/llvmorg-21.1.8/LLVM-21.1.8-woa64.exe" -OutFile "$env:TEMP\LLVM-21.1.8-woa64.exe"
+> Start-Process -FilePath "$env:TEMP\LLVM-21.1.8-woa64.exe" -ArgumentList "/S" -Wait
 ```
 
 <Callout type="info">
@@ -70,21 +70,22 @@ Start-Process -FilePath "$env:TEMP\LLVM-21.1.8-woa64.exe" -ArgumentList "/S" -Wa
 
 If you intend on building WebKit locally (optional, x64 only), you should install these packages:
 
-```ps1 Scoop
-scoop install make cygwin python
+```ps1 title="Scoop"
+> scoop install make cygwin python
 ```
 
 <Callout type="info">Cygwin is not required for ARM64 builds as WebKit is provided as a pre-built binary.</Callout>
 
 From here on out, it is **expected you use a PowerShell Terminal with `.\scripts\vs-shell.ps1` sourced**. This script is available in the Bun repository and can be loaded by executing it:
 
-```ps1 title=".\scripts\vs-shell.ps1"
+```ps1
+> .\scripts\vs-shell.ps1
 ```
 
 To verify, you can check for an MSVC-only command line such as `mt.exe`
 
 ```ps1
-Get-Command mt
+> Get-Command mt
 ```
 
 <Callout type="info">
@@ -95,16 +96,16 @@ Get-Command mt
 ## Building
 
 ```ps1
-bun run build
+> bun run build
 
 # after the initial `bun run build` you can use the following to build
-ninja -Cbuild/debug
+> ninja -Cbuild/debug
 ```
 
 If this was successful, you should have a `bun-debug.exe` in the `build/debug` folder.
 
 ```ps1
-.\build\debug\bun-debug.exe --revision
+> .\build\debug\bun-debug.exe --revision
 ```
 
 You should add this to `$Env:PATH`. The simplest way to do so is to open the start menu, type "Path", and then navigate the environment variables menu to add `C:\.....\bun\build\debug` to the user environment variable `PATH`. You should then restart your editor (if it does not update still, log out and log back in).
@@ -120,15 +121,15 @@ You can run the test suite either using `bun test <path>` or by using the wrappe
 
 ```ps1
 # Setup
-bun i --cwd packages\bun-internal-test
+> bun i --cwd packages\bun-internal-test
 
 # Run the entire test suite with reporter
 # the package.json script "test" uses "build/debug/bun-debug.exe" by default
-bun run test
+> bun run test
 
 # Run an individual test file:
-bun-debug test node\fs
-bun-debug test "C:\bun\test\js\bun\resolve\import-meta.test.js"
+> bun-debug test node\fs
+> bun-debug test "C:\bun\test\js\bun\resolve\import-meta.test.js"
 ```
 
 ## Troubleshooting
diff --git b/content/docs/project/contributing.mdx a/content/docs/project/contributing.mdx
index 3bce6928..693eae2b 100644
--- b/content/docs/project/contributing.mdx
+++ a/content/docs/project/contributing.mdx
@@ -12,7 +12,7 @@ If you are using Windows, please refer to [this guide](/docs/project/building-wi
 A Nix flake is provided as an alternative to manual dependency installation:
 
 ```bash
-nix develop
+$ nix develop
 # or explicitly use the pure shell
 # nix develop .#pure
 $ export CMAKE_SYSTEM_PROCESSOR=$(uname -m)
@@ -115,7 +115,7 @@ If none of the above solutions apply, you will have to install it [manually](htt
 Make sure Clang/LLVM 21 is in your path:
 
 ```bash
-which clang-21
+$ which clang-21
 ```
 
 If not, run this to manually add it:
@@ -146,7 +146,7 @@ $ bun run build
 The binary will be located at `./build/debug/bun-debug`. It is recommended to add this to your `$PATH`. To verify the build worked, let's print the version number on the development build of Bun.
 
 ```bash
-build/debug/bun-debug --version
+$ build/debug/bun-debug --version
 x.y.z_debug
 ```
 
@@ -344,7 +344,7 @@ $ brew install pkg-config
 If you see this error when compiling, run:
 
 ```bash
-xcode-select --install
+$ xcode-select --install
 ```
 
 ### Cannot find `libatomic.a`
diff --git b/content/docs/quickstart.mdx a/content/docs/quickstart.mdx
index 805030ba..24347a1f 100644
--- b/content/docs/quickstart.mdx
+++ a/content/docs/quickstart.mdx
@@ -26,7 +26,6 @@ Build a minimal HTTP server with `Bun.serve`, run it locally, then evolve it by
 
     ```bash title="terminal"
     $ bun init my-app
-    
     ✓ Select a project template: Blank
 
     - .gitignore
@@ -49,7 +48,6 @@ Build a minimal HTTP server with `Bun.serve`, run it locally, then evolve it by
     ```bash title="terminal"
     $ cd my-app
     $ bun run index.ts
-    
     Hello via Bun!
     ```
 
@@ -77,7 +75,6 @@ Build a minimal HTTP server with `Bun.serve`, run it locally, then evolve it by
 
     ```bash title="terminal"
     $ bun run index.ts
-    
     Listening on http://localhost:3000
     ```
 
@@ -112,7 +109,7 @@ Build a minimal HTTP server with `Bun.serve`, run it locally, then evolve it by
       ```
 
     </Accordion>
-</Accordions>
+    </Accordions>
 
   </Step>
   <Step>
@@ -149,7 +146,6 @@ Build a minimal HTTP server with `Bun.serve`, run it locally, then evolve it by
 
     ```bash title="terminal"
     $ bun run index.ts
-    
     Listening on http://localhost:3000
     ```
 
@@ -208,7 +204,6 @@ Build a minimal HTTP server with `Bun.serve`, run it locally, then evolve it by
 
     ```bash title="terminal"
     $ bun run index.ts
-    
     Listening on http://localhost:3000
     ```
 
@@ -249,7 +244,6 @@ Then run it with `bun run start`.
 
 ```bash title="terminal"
 $ bun run start
-
 Listening on http://localhost:3000
 ```
 
diff --git b/content/docs/runtime/bunfig.mdx a/content/docs/runtime/bunfig.mdx
index c3f01343..4bdbc284 100644
--- b/content/docs/runtime/bunfig.mdx
+++ a/content/docs/runtime/bunfig.mdx
@@ -212,7 +212,6 @@ To specify a coverage threshold. By default, no threshold is set. If your test s
 
 ```toml title="bunfig.toml"
 [test]
-
 # to require 90% line-level and function-level coverage
 coverageThreshold = 0.9
 ```
@@ -257,7 +256,7 @@ By default, coverage reports will be printed to the console. For persistent code
 
 ```toml title="bunfig.toml"
 [test]
-coverageReporter  = ["text", "lcov"]  # default ["text"]
+coverageReporter = ["text", "lcov"]  # default ["text"]
 ```
 
 ### `test.coverageDir`
@@ -562,7 +561,6 @@ To configure the cache behavior:
 
 ```toml title="bunfig.toml"
 [install.cache]
-
 # the directory to use for the cache
 dir = "~/.bun/install/cache"
 
@@ -748,9 +746,7 @@ With this option, the command being run will not be printed to the console:
 
 ```sh
 $ bun run dev
-```
 
-```txt
 Running "dev"...
 ```
 
diff --git b/content/docs/runtime/child-process.mdx a/content/docs/runtime/child-process.mdx
index 93f16aba..144a7c3e 100644
--- b/content/docs/runtime/child-process.mdx
+++ a/content/docs/runtime/child-process.mdx
@@ -100,7 +100,7 @@ You can read results from the subprocess via the `stdout` and `stderr` propertie
 ```ts
 const proc = Bun.spawn(["bun", "--version"]);
 const text = await proc.stdout.text();
-console.log(text); // => "1.3.3\n"
+console.log(text); // => "$BUN_LATEST_VERSION\n"
 ```
 
 Configure the output stream by passing one of the following values to `stdout/stderr`:
diff --git b/content/docs/runtime/cron.mdx a/content/docs/runtime/cron.mdx
index 49796025..315383b7 100644
--- b/content/docs/runtime/cron.mdx
+++ a/content/docs/runtime/cron.mdx
@@ -266,18 +266,18 @@ When the cron daemon fires the job, Bun imports your module and calls the `sched
 **Viewing registered jobs:**
 
 ```sh
-crontab -l
+$ crontab -l
 ```
 
 **Logs:** On Linux, cron output goes to the system log. Check with:
 
 ```sh
 # systemd-based (Ubuntu, Fedora, Arch, etc.)
-journalctl -u cron       # or crond on some distros
-journalctl -u cron --since "1 hour ago"
+$ journalctl -u cron       # or crond on some distros
+$ journalctl -u cron --since "1 hour ago"
 
 # syslog-based (older systems)
-grep CRON /var/log/syslog
+$ grep CRON /var/log/syslog
 ```
 
 To capture stdout/stderr to a file, redirect output in the crontab entry directly, or add logging inside your `scheduled()` handler.
@@ -287,10 +287,10 @@ To capture stdout/stderr to a file, redirect output in the crontab entry directl
 ```sh
 # Edit your crontab and remove the "# bun-cron: <title>" comment
 # and the command line below it
-crontab -e
+$ crontab -e
 
 # Or remove ALL bun cron jobs at once by filtering them out:
-crontab -l | grep -v "# bun-cron:" | grep -v "\-\-cron-title=" | crontab -
+$ crontab -l | grep -v "# bun-cron:" | grep -v "\-\-cron-title=" | crontab -
 ```
 
 ### macOS
@@ -306,7 +306,7 @@ The plist uses `StartCalendarInterval` to define the schedule. Complex patterns
 **Viewing registered jobs:**
 
 ```sh
-launchctl list | grep bun.cron
+$ launchctl list | grep bun.cron
 ```
 
 **Logs:** stdout and stderr are written to:
@@ -319,22 +319,22 @@ launchctl list | grep bun.cron
 For example, a job titled `weekly-report`:
 
 ```sh
-cat /tmp/bun.cron.weekly-report.stdout.log
-tail -f /tmp/bun.cron.weekly-report.stderr.log
+$ cat /tmp/bun.cron.weekly-report.stdout.log
+$ tail -f /tmp/bun.cron.weekly-report.stderr.log
 ```
 
 **Manually uninstalling without code:**
 
 ```sh
 # Unload the job from launchd
-launchctl bootout gui/$(id -u)/bun.cron.<title>
+$ launchctl bootout gui/$(id -u)/bun.cron.<title>
 
 # Delete the plist file
 $ rm ~/Library/LaunchAgents/bun.cron.<title>.plist
 
 # Example for a job titled "weekly-report":
-launchctl bootout gui/$(id -u)/bun.cron.weekly-report
-rm ~/Library/LaunchAgents/bun.cron.weekly-report.plist
+$ launchctl bootout gui/$(id -u)/bun.cron.weekly-report
+$ rm ~/Library/LaunchAgents/bun.cron.weekly-report.plist
 ```
 
 ### Windows
diff --git b/content/docs/runtime/debugger.mdx a/content/docs/runtime/debugger.mdx
index 5d65b594..17abb2c9 100644
--- b/content/docs/runtime/debugger.mdx
+++ a/content/docs/runtime/debugger.mdx
@@ -22,9 +22,7 @@ To enable debugging when running code with Bun, use the `--inspect` flag. This a
 
 ```sh title="terminal"
 $ bun --inspect server.ts
-```
 
-```txt id="terminal"
 ------------------ Bun Inspector ------------------
 Listening at:
   ws://localhost:6499/0tqxs9exrgrm
@@ -143,12 +141,16 @@ await fetch("https://example.com", {
   },
   body: JSON.stringify({ foo: "bar" }),
 });
+```
 
-[fetch] $ curl --http1.1 "https://example.com/" -X POST -H "content-type: application/json" -H "Connection: keep-alive" -H "User-Agent: Bun/1.3.3" -H "Accept: */*" -H "Host: example.com" -H "Accept-Encoding: gzip, deflate, br" --compressed -H "Content-Length: 13" --data-raw "{\"foo\":\"bar\"}"
+This prints the `fetch` request as a single-line `curl` command to let you copy-paste into your terminal to replicate the request.
+
+```sh
+[fetch] $ curl --http1.1 "https://example.com/" -X POST -H "content-type: application/json" -H "Connection: keep-alive" -H "User-Agent: Bun/$BUN_LATEST_VERSION" -H "Accept: */*" -H "Host: example.com" -H "Accept-Encoding: gzip, deflate, br" --compressed -H "Content-Length: 13" --data-raw "{\"foo\":\"bar\"}"
 [fetch] > HTTP/1.1 POST https://example.com/
 [fetch] > content-type: application/json
 [fetch] > Connection: keep-alive
-[fetch] > User-Agent: Bun/1.3.3
+[fetch] > User-Agent: Bun/$BUN_LATEST_VERSION
 [fetch] > Accept: */*
 [fetch] > Host: example.com
 [fetch] > Accept-Encoding: gzip, deflate, br
@@ -184,11 +186,13 @@ await fetch("https://example.com", {
 });
 ```
 
-```txt
+This prints the following to the console:
+
+```sh title="terminal"
 [fetch] > HTTP/1.1 POST https://example.com/
 [fetch] > content-type: application/json
 [fetch] > Connection: keep-alive
-[fetch] > User-Agent: Bun/1.3.3
+[fetch] > User-Agent: Bun/$BUN_LATEST_VERSION
 [fetch] > Accept: */*
 [fetch] > Host: example.com
 [fetch] > Accept-Encoding: gzip, deflate, br
@@ -314,7 +318,9 @@ const fn = () => {
 fn();
 ```
 
-```txt
+This logs the following:
+
+```sh title="terminal"
 Error: here!
     at myInner (file.js:4:15)
     at fn (file.js:8:5)
diff --git b/content/docs/runtime/environment-variables.mdx a/content/docs/runtime/environment-variables.mdx
index bafdcbb1..a0ebf1bc 100644
--- b/content/docs/runtime/environment-variables.mdx
+++ a/content/docs/runtime/environment-variables.mdx
@@ -29,7 +29,7 @@ $ FOO=helloworld bun run dev
 $ set FOO=helloworld && bun run dev
 
 # Using PowerShell
-$env:FOO="helloworld"; bun run dev
+$ $env:FOO="helloworld"; bun run dev
 ```
 
 <Accordions type="single">
diff --git b/content/docs/runtime/ffi.mdx a/content/docs/runtime/ffi.mdx
index 6967cd7c..9d5c0613 100644
--- b/content/docs/runtime/ffi.mdx
+++ a/content/docs/runtime/ffi.mdx
@@ -66,7 +66,7 @@ pub export fn add(a: i32, b: i32) i32 {
 To compile:
 
 ```bash title="terminal"
-zig build-lib add.zig -dynamic -OReleaseFast
+$ zig build-lib add.zig -dynamic -OReleaseFast
 ```
 
 Pass a path to the shared library and a map of symbols to import into `dlopen`:
@@ -100,7 +100,7 @@ pub extern "C" fn add(a: i32, b: i32) -> i32 {
 To compile:
 
 ```bash
-rustc --crate-type cdylib add.rs
+$ rustc --crate-type cdylib add.rs
 ```
 
 ### C++
@@ -116,7 +116,7 @@ extern "C" int32_t add(int32_t a, int32_t b) {
 To compile:
 
 ```bash
-zig build-lib add.cpp -dynamic -lc -lc++
+$ zig build-lib add.cpp -dynamic -lc -lc++
 ```
 
 ---
diff --git b/content/docs/runtime/file-types.mdx a/content/docs/runtime/file-types.mdx
index 934c4f43..1d1913cc 100644
--- b/content/docs/runtime/file-types.mdx
+++ a/content/docs/runtime/file-types.mdx
@@ -310,7 +310,7 @@ The html loader processes HTML files and bundles any referenced assets. It will:
 
 For example, given this HTML file:
 
-```html title="src/index.html"
+```html tab="src/index.html"
 <!DOCTYPE html>
 <html>
   <body>
@@ -323,7 +323,7 @@ For example, given this HTML file:
 
 It will output a new HTML file with the bundled assets:
 
-```html title="dist/output.html"
+```html tab="dist/output.html"
 <!DOCTYPE html>
 <html>
   <body>
diff --git b/content/docs/runtime/http/server.mdx a/content/docs/runtime/http/server.mdx
index 62c93330..2dda6d9a 100644
--- b/content/docs/runtime/http/server.mdx
+++ a/content/docs/runtime/http/server.mdx
@@ -502,7 +502,7 @@ export interface Post {
 
 ## Reference
 
-```ts See TypeScript Definitions
+```ts
 interface Server extends Disposable {
   /**
    * Stop the server from accepting new connections.
diff --git b/content/docs/runtime/http/websockets.mdx a/content/docs/runtime/http/websockets.mdx
index ddb815ba..396d5a3e 100644
--- b/content/docs/runtime/http/websockets.mdx
+++ a/content/docs/runtime/http/websockets.mdx
@@ -92,15 +92,15 @@ Bun.serve({
 
 Each `ServerWebSocket` instance has a `.send()` method for sending messages to the client. It supports a range of input types.
 
-```ts title="server.ts" focus={4-6}
+```ts title="server.ts"
 Bun.serve({
   fetch(req, server) {}, // upgrade logic
   websocket: {
-    message(ws, message) {
-      ws.send("Hello world"); // string
-      ws.send(response.arrayBuffer()); // ArrayBuffer
-      ws.send(new Uint8Array([1, 2, 3])); // TypedArray | DataView
-    },
+    message(ws, message) { // [!code highlight]
+      ws.send("Hello world"); // string // [!code highlight]
+      ws.send(response.arrayBuffer()); // ArrayBuffer // [!code highlight]
+      ws.send(new Uint8Array([1, 2, 3])); // TypedArray | DataView // [!code highlight]
+    }, // [!code highlight]
   },
 });
 ```
diff --git b/content/docs/runtime/index.mdx a/content/docs/runtime/index.mdx
index 0618d886..dbf0c460 100644
--- b/content/docs/runtime/index.mdx
+++ a/content/docs/runtime/index.mdx
@@ -64,7 +64,7 @@ Flags that occur at the end of the command will be ignored and passed through to
 Compare to `npm run <script>` or `yarn <script>`
 </Callout>
 
-```sh
+```sh title="terminal"
 $ bun [bun flags] run <script> [script flags]
 ```
 
diff --git b/content/docs/runtime/markdown.mdx a/content/docs/runtime/markdown.mdx
index 684a7d12..b0d5d2f5 100644
--- b/content/docs/runtime/markdown.mdx
+++ a/content/docs/runtime/markdown.mdx
@@ -3,8 +3,8 @@ title: Markdown
 description: Parse and render Markdown with Bun's built-in Markdown API, supporting GFM extensions and custom rendering callbacks
 ---
 
-<Callout type="note">
-  **Unstable API** — This API is under active development and may change in future versions of Bun.
+<Callout type="info">
+**Unstable API** — This API is under active development and may change in future versions of Bun.
 </Callout>
 
 Bun includes a fast, built-in Markdown parser written in Zig. It supports GitHub Flavored Markdown (GFM) extensions and provides three APIs:
diff --git b/content/docs/runtime/networking/fetch.mdx a/content/docs/runtime/networking/fetch.mdx
index 0525926b..1116e0d6 100644
--- b/content/docs/runtime/networking/fetch.mdx
+++ a/content/docs/runtime/networking/fetch.mdx
@@ -358,7 +358,7 @@ This will print the request and response headers to your terminal:
 ```sh
 [fetch] > HTTP/1.1 GET http://example.com/
 [fetch] > Connection: keep-alive
-[fetch] > User-Agent: Bun/1.3.3
+[fetch] > User-Agent: Bun/$BUN_LATEST_VERSION
 [fetch] > Accept: */*
 [fetch] > Host: example.com
 [fetch] > Accept-Encoding: gzip, deflate, br, zstd
diff --git b/content/docs/runtime/plugins.mdx a/content/docs/runtime/plugins.mdx
index 747ff123..16d2f508 100644
--- b/content/docs/runtime/plugins.mdx
+++ a/content/docs/runtime/plugins.mdx
@@ -330,7 +330,7 @@ To create a native plugin, you must export a C ABI function which matches the si
 
 ```bash title="terminal"
 $ bun add -g @napi-rs/cli
-napi new
+$ napi new
 ```
 
 Then install this crate:
diff --git b/content/docs/runtime/sql.mdx a/content/docs/runtime/sql.mdx
index 75841232..7360ec11 100644
--- b/content/docs/runtime/sql.mdx
+++ a/content/docs/runtime/sql.mdx
@@ -524,11 +524,11 @@ MySQL connections can be configured via environment variables:
 
 ```bash
 # Primary connection URL (checked first)
-$ MYSQL_URL="mysql://user:pass@localhost:3306/mydb"
+MYSQL_URL="mysql://user:pass@localhost:3306/mydb"
 
 # Alternative: DATABASE_URL with MySQL protocol
-$ DATABASE_URL="mysql://user:pass@localhost:3306/mydb"
-$ DATABASE_URL="mysql2://user:pass@localhost:3306/mydb"
+DATABASE_URL="mysql://user:pass@localhost:3306/mydb"
+DATABASE_URL="mysql2://user:pass@localhost:3306/mydb"
 ```
 
 If no connection URL is provided, MySQL checks these individual parameters:
@@ -572,9 +572,9 @@ SQLite connections can be configured via `DATABASE_URL` when it contains a SQLit
 
 ```bash
 # These are all recognized as SQLite
-$ DATABASE_URL=":memory:"
-$ DATABASE_URL="sqlite://./app.db"
-$ DATABASE_URL="file:///absolute/path/to/db.sqlite"
+DATABASE_URL=":memory:"
+DATABASE_URL="sqlite://./app.db"
+DATABASE_URL="file:///absolute/path/to/db.sqlite"
 ```
 
 **Note:** PostgreSQL-specific environment variables (`POSTGRES_URL`, `PGHOST`, etc.) are ignored when using SQLite.
diff --git b/content/docs/runtime/sqlite.mdx a/content/docs/runtime/sqlite.mdx
index 02bb47d3..8ff91bb9 100644
--- b/content/docs/runtime/sqlite.mdx
+++ a/content/docs/runtime/sqlite.mdx
@@ -10,11 +10,7 @@ import { Database } from "bun:sqlite";
 
 const db = new Database(":memory:");
 const query = db.query("select 'Hello world' as message;");
-query.get();
-```
-
-```txt
-{ message: "Hello world" }
+query.get(); // => { message: "Hello world" }
 ```
 
 The API is synchronous and fast. Credit to [better-sqlite3](https://github.com/JoshuaWise/better-sqlite3) and its contributors for inspiring the API of `bun:sqlite`.
@@ -143,14 +139,10 @@ import { Database } from "bun:sqlite";
 {
   using db = new Database("mydb.sqlite"); // [!code highlight]
   using query = db.query("select 'Hello world' as message;"); // [!code highlight]
-  console.log(query.get());
+  console.log(query.get()); // => { message: "Hello world" }
 }
 ```
 
-```txt
-{ message: "Hello world" }
-```
-
 ### `.serialize()`
 
 `bun:sqlite` supports SQLite's built-in mechanism for [serializing](https://www.sqlite.org/c3ref/serialize.html) and [deserializing](https://www.sqlite.org/c3ref/deserialize.html) databases to and from memory.
@@ -305,10 +297,7 @@ Use `.all()` to run a query and get back the results as an array of objects.
 ```ts title="db.ts"
 const query = db.query(`select $message;`);
 query.all({ $message: "Hello world" }); // [!code highlight]
-```
-
-```txt
-[{ message: "Hello world" }]
+// => [{ message: "Hello world" }]
 ```
 
 Internally, this calls [`sqlite3_reset`](https://www.sqlite.org/capi3ref.html#sqlite3_reset) and repeatedly calls [`sqlite3_step`](https://www.sqlite.org/capi3ref.html#sqlite3_step) until it returns `SQLITE_DONE`.
@@ -320,10 +309,7 @@ Use `.get()` to run a query and get back the first result as an object.
 ```ts title="db.ts"
 const query = db.query(`select $message;`);
 query.get({ $message: "Hello world" }); // [!code highlight]
-```
-
-```txt
-{ $message: "Hello world" }
+// => { $message: "Hello world" }
 ```
 
 Internally, this calls [`sqlite3_reset`](https://www.sqlite.org/capi3ref.html#sqlite3_reset) followed by [`sqlite3_step`](https://www.sqlite.org/capi3ref.html#sqlite3_step) until it no longer returns `SQLITE_ROW`. If the query returns no rows, `undefined` is returned.
@@ -335,13 +321,10 @@ Use `.run()` to run a query and get back an object with execution metadata. This
 ```ts title="db.ts"
 const query = db.query(`create table foo;`);
 query.run(); // [!code highlight]
-```
-
-```txt
-{
-  lastInsertRowid: 0,
-  changes: 0,
-}
+// {
+//   lastInsertRowid: 0,
+//   changes: 0,
+// }
 ```
 
 Internally, this calls [`sqlite3_reset`](https://www.sqlite.org/capi3ref.html#sqlite3_reset) and calls [`sqlite3_step`](https://www.sqlite.org/capi3ref.html#sqlite3_step) once. Stepping through all the rows is not necessary when you don't care about the results.
@@ -366,13 +349,8 @@ const query = db.query("SELECT title, year FROM movies").as(Movie); // [!code hi
 const movies = query.all();
 const first = query.get();
 
-console.log(movies[0].isMarvel);
-console.log(first.isMarvel);
-```
-
-```txt
-true
-true
+console.log(movies[0].isMarvel); // => true
+console.log(first.isMarvel); // => true
 ```
 
 As a performance optimization, the class constructor is not called, default initializers are not run, and private fields are not accessible. This is more like using `Object.create` than `new`. The class's prototype is assigned to the object, methods are attached, and getters/setters are set up, but the constructor is not called.
@@ -408,14 +386,11 @@ const query = db.query(`select $message;`);
 
 query.values({ $message: "Hello world" }); // [!code highlight]
 query.values(2); // [!code highlight]
-```
-
-```txt
-[
-  [ "Iron Man", 2008 ],
-  [ "The Avengers", 2012 ],
-  [ "Ant-Man: Quantumania", 2023 ],
-]
+// [
+//   [ "Iron Man", 2008 ],
+//   [ "The Avengers", 2012 ],
+//   [ "Ant-Man: Quantumania", 2023 ],
+// ]
 ```
 
 Internally, this calls [`sqlite3_reset`](https://www.sqlite.org/capi3ref.html#sqlite3_reset) and repeatedly calls [`sqlite3_step`](https://www.sqlite.org/capi3ref.html#sqlite3_step) until it returns `SQLITE_DONE`.
@@ -473,13 +448,8 @@ const query = db.query("SELECT ?1, ?2");
 const results = query.all("hello", "goodbye");
 ```
 
-```txt
-[
-	{
-		"?1": "hello",
-		"?2": "goodbye",
-	},
-];
+```json
+[{"?1": "hello", "?2": "goodbye"}]
 ```
 
 ---
@@ -502,12 +472,7 @@ import { Database } from "bun:sqlite";
 const db = new Database(":memory:", { safeIntegers: true }); // [!code highlight]
 const query = db.query(`SELECT ${BigInt(Number.MAX_SAFE_INTEGER) + 102n} as max_int`);
 const result = query.get();
-
-console.log(result.max_int);
-```
-
-```txt
-9007199254741093n
+console.log(result.max_int); // => 9007199254741093n
 ```
 
 When `safeIntegers` is `true`, `bun:sqlite` will throw an error if a `bigint` value in a bound parameter exceeds 64 bits:
@@ -523,14 +488,10 @@ const query = db.query("INSERT INTO test (value) VALUES ($value)");
 try {
   query.run({ $value: BigInt(Number.MAX_SAFE_INTEGER) ** 2n });
 } catch (e) {
-  console.log(e.message);
+  console.log(e.message); // => BigInt value '81129638414606663681390495662081' is out of range
 }
 ```
 
-```txt
-BigInt value '81129638414606663681390495662081' is out of range
-```
-
 ### `safeIntegers: false` (default)
 
 When `safeIntegers` is `false`, `bun:sqlite` will return integers as `number` types and truncate any bits beyond 53:
@@ -541,11 +502,7 @@ import { Database } from "bun:sqlite";
 const db = new Database(":memory:", { safeIntegers: false }); // [!code highlight]
 const query = db.query(`SELECT ${BigInt(Number.MAX_SAFE_INTEGER) + 102n} as max_int`);
 const result = query.get();
-console.log(result.max_int);
-```
-
-```txt
-9007199254741092
+console.log(result.max_int); // => 9007199254741092
 ```
 
 ---
@@ -638,7 +595,7 @@ db.loadExtension("myext"); // [!code highlight]
 
 ```bash title="terminal"
 $ brew install sqlite
-which sqlite # get path to binary
+$ which sqlite # get path to binary
 ```
 
 To point `bun:sqlite` to the new build, call `Database.setCustomSQLite(path)` before creating any `Database` instances. (On other operating systems, this is a no-op.) Pass a path to the SQLite `.dylib` file, _not_ the executable. With recent versions of Homebrew this is something like `/opt/homebrew/Cellar/sqlite/<version>/libsqlite3.dylib`.
diff --git b/content/docs/runtime/streams.mdx a/content/docs/runtime/streams.mdx
index a829e58a..74129f2b 100644
--- b/content/docs/runtime/streams.mdx
+++ a/content/docs/runtime/streams.mdx
@@ -195,7 +195,7 @@ sink.start({
 
 ## Reference
 
-```ts See Typescript Definitions
+```ts title="See Typescript Definitions"
 /**
  * Fast incremental writer that becomes an `ArrayBuffer` on end().
  */
diff --git b/content/docs/runtime/templating/create.mdx a/content/docs/runtime/templating/create.mdx
index 4259a1a8..9edd678f 100644
--- b/content/docs/runtime/templating/create.mdx
+++ a/content/docs/runtime/templating/create.mdx
@@ -80,7 +80,7 @@ plugins = ["bun-plugin-tailwind"]
 
 And a `${component}.css` file with `@import "tailwindcss";` at the top:
 
-```scss title="MyComponent.css"
+```css title="MyComponent.css"
 @import "tailwindcss";
 ```
 
@@ -230,7 +230,7 @@ After cloning a template, `bun create` will automatically remove the `"bun-creat
 | `GITHUB_TOKEN` (or `GITHUB_ACCESS_TOKEN`) | This lets `bun create` work with private repositories or if you get rate-limited. `GITHUB_TOKEN` is chosen over `GITHUB_ACCESS_TOKEN` if both exist. |
 
 <Accordions type="single">
-<Accordion title={<span>How <code>bun create</code> works</span>}>
+<Accordion title={<span>How <code>bun create</code> works</span>} id="how-bun-create-works">
 
 When you run `bun create ${template} ${destination}`, here’s what happens:
 
diff --git b/content/docs/runtime/transpiler.mdx a/content/docs/runtime/transpiler.mdx
index e5e941fd..5eff589b 100644
--- b/content/docs/runtime/transpiler.mdx
+++ a/content/docs/runtime/transpiler.mdx
@@ -17,7 +17,7 @@ const transpiler = new Bun.Transpiler({
 
 Transpile code synchronously with the `.transformSync()` method. Modules are not resolved and the code is not executed. The result is a string of vanilla JavaScript code.
 
-```ts tab="transpile.ts"
+```ts tab="transpile.ts" 
 const transpiler = new Bun.Transpiler({
   loader: 'tsx',
 });
@@ -56,11 +56,9 @@ transpiler.transformSync("<div>hi!</div>", "tsx");
 
 <Accordions type="single">
 <Accordion title="Nitty gritty">
-
 When `.transformSync` is called, the transpiler is run in the same thread as the currently executed code.
 
 If a macro is used, it will be run in the same thread as the transpiler, but in a separate event loop from the rest of your application. Currently, globals between macros and regular code are shared, which means it is possible (but not recommended) to share states between macros and regular code. Attempting to use AST nodes outside of a macro is undefined behavior.
-
 </Accordion>
 </Accordions>
 
@@ -186,7 +184,7 @@ const result = transpiler.scanImports(code);
 
 ## Reference
 
-```ts See Typescript Definitions
+```ts
 type Loader = "jsx" | "js" | "ts" | "tsx";
 
 interface TranspilerOptions {
diff --git b/content/docs/runtime/utils.mdx a/content/docs/runtime/utils.mdx
index 9cab968f..8e37e420 100644
--- b/content/docs/runtime/utils.mdx
+++ a/content/docs/runtime/utils.mdx
@@ -9,7 +9,7 @@ A `string` containing the version of the `bun` CLI that is currently running.
 
 ```ts title="terminal"
 Bun.version;
-// => "1.3.3"
+// => "$BUN_LATEST_VERSION"
 ```
 
 ## `Bun.revision`
@@ -333,8 +333,8 @@ existing code can be ported to Bun and vice versa.
 
 [In this benchmark](https://github.com/oven-sh/bun/blob/5147c0ba7379d85d4d1ed0714b84d6544af917eb/bench/snippets/string-width.mjs#L13), `Bun.stringWidth` is a ~6,756x faster than the `string-width` npm package for input larger than about 500 characters. Big thanks to [sindresorhus](https://github.com/sindresorhus) for their work on `string-width`!
 
-```ts
-❯ bun string-width.mjs
+```bash
+$ bun string-width.mjs
 cpu: 13th Gen Intel(R) Core(TM) i9-13900
 runtime: bun 1.0.29 (x64-linux)
 
@@ -342,8 +342,7 @@ benchmark                                          time (avg)             (min 
 ------------------------------------------------------------------------------------- -----------------------------
 Bun.stringWidth     500 chars ascii              37.09 ns/iter   (36.77 ns … 41.11 ns)  37.07 ns  38.84 ns  38.99 ns
 
-❯ node string-width.mjs
-
+$ node string-width.mjs
 benchmark                                          time (avg)             (min … max)       p75       p99      p995
 ------------------------------------------------------------------------------------- -----------------------------
 npm/string-width    500 chars ascii             249,710 ns/iter (239,970 ns … 293,180 ns) 250,930 ns  276,700 ns 281,450 ns
@@ -363,7 +362,7 @@ As a reminder, 1 nanosecond (ns) is 1 billionth of a second. Here's a reference
 | ms   | 1             |
 
 ```bash title="terminal"
-❯ bun string-width.mjs
+$ bun string-width.mjs
 cpu: 13th Gen Intel(R) Core(TM) i9-13900
 runtime: bun 1.0.29 (x64-linux)
 
@@ -392,7 +391,7 @@ Bun.stringWidth 95,000 chars ansi+emoji+ascii  572.69 µs/iter (565.52 µs … 6
 ```
 
 ```bash title="terminal"
-❯ node string-width.mjs
+$ node string-width.mjs
 cpu: 13th Gen Intel(R) Core(TM) i9-13900
 runtime: node v21.4.0 (x64-linux)
 
diff --git b/content/docs/runtime/watch-mode.mdx a/content/docs/runtime/watch-mode.mdx
index 99d7f89c..4d2061ae 100644
--- b/content/docs/runtime/watch-mode.mdx
+++ a/content/docs/runtime/watch-mode.mdx
@@ -117,8 +117,6 @@ If you run this file with `bun --hot server.ts`, you'll see the reload count inc
 ```bash title="terminal"
 $ bun --hot index.ts
 
-bun test v$BUN_LATEST_VERSION (9c68abdb)
-
 Reloaded 1 times
 Reloaded 2 times
 Reloaded 3 times
diff --git b/content/docs/test/dom.mdx a/content/docs/test/dom.mdx
index 20af4d98..612222ba 100644
--- b/content/docs/test/dom.mdx
+++ a/content/docs/test/dom.mdx
@@ -62,9 +62,6 @@ Let's run this test with `bun test`:
 
 ```bash title="terminal"
 $ bun test
-```
-
-```
 bun test v$BUN_LATEST_VERSION
 
 dom.test.ts:
diff --git b/content/docs/test/index.mdx a/content/docs/test/index.mdx
index 5409bf2d..d0509f02 100644
--- b/content/docs/test/index.mdx
+++ a/content/docs/test/index.mdx
@@ -249,8 +249,6 @@ When using `--randomize`, the seed used for randomization will be displayed in t
 ```sh title="terminal"
 $ bun test --randomize
 
-bun test v$BUN_LATEST_VERSION (9c68abdb)
-
 # ... test output ...
  --seed=12345
  2 pass
diff --git b/content/docs/test/lifecycle.mdx a/content/docs/test/lifecycle.mdx
index 7d975106..95761055 100644
--- b/content/docs/test/lifecycle.mdx
+++ a/content/docs/test/lifecycle.mdx
@@ -265,8 +265,6 @@ describe("outer describe", () => {
   });
 });
 
-bun test v$BUN_LATEST_VERSION (9c68abdb)
-
 // Output order:
 // File beforeAll
 // Outer beforeAll
diff --git b/content/docs/test/reporters.mdx a/content/docs/test/reporters.mdx
index 39c9da13..1a22d4c8 100644
--- b/content/docs/test/reporters.mdx
+++ a/content/docs/test/reporters.mdx
@@ -1,6 +1,5 @@
 ---
 title: Test Reporters
-description:
 ---
 
 bun test supports different output formats through reporters. This document covers both built-in reporters and how to implement your own custom reporters.
@@ -98,7 +97,7 @@ The JUnit reporter currently has a few limitations that will be addressed in fut
 
 Bun test automatically detects when it's running inside GitHub Actions and emits GitHub Actions annotations to the console directly. No special configuration is needed beyond installing Bun and running `bun test`.
 
-For a GitHub Actions workflow configuration example, see the [CI/CD integration](/docs/pm/cli/install#ci%2Fcd) section of the CLI documentation.
+For a GitHub Actions workflow configuration example, see the [CI/CD integration](/docs/pm/cli/install#ci-cd) section of the CLI documentation.
 
 ---
 
diff --git b/content/docs/test/runtime-behavior.mdx a/content/docs/test/runtime-behavior.mdx
index 66ae01c0..23ca3a86 100644
--- b/content/docs/test/runtime-behavior.mdx
+++ a/content/docs/test/runtime-behavior.mdx
@@ -275,10 +275,10 @@ The test runner properly handles common signals:
 
 ```bash title="terminal"
 # Gracefully stops test execution
-kill -SIGTERM <test-process-pid>
+$ kill -SIGTERM <test-process-pid>
 
 # Immediately stops test execution
-kill -SIGKILL <test-process-pid>
+$ kill -SIGKILL <test-process-pid>
 ```
 
 ### Environment Detection
diff --git b/content/docs/typescript.mdx a/content/docs/typescript.mdx
index 862870fc..ad639115 100644
--- b/content/docs/typescript.mdx
+++ a/content/docs/typescript.mdx
@@ -5,8 +5,8 @@ description: Using TypeScript with Bun, including type definitions and compiler
 
 To install the TypeScript definitions for Bun's built-in APIs, install `@types/bun`.
 
-```zsh title="terminal"
-$ bun add -d @types/bun # dev dependency
+```sh title="terminal"
+$ bun add -d @types/bun  # dev dependency
 ```
 
 At this point, you should be able to reference the `Bun` global in your TypeScript files without seeing errors in your editor.
@@ -15,7 +15,7 @@ At this point, you should be able to reference the `Bun` global in your TypeScri
 
 Bun supports things like top-level await, JSX, and extensioned `.ts` imports, which TypeScript doesn't allow by default. Below is a set of recommended `compilerOptions` for a Bun project, so you can use these features without seeing compiler warnings from TypeScript.
 
-```json title="tsconfig.json"
+```jsonc title="tsconfig.json"
 {
   "compilerOptions": {
     // Environment setup & latest features
diff --git b/content/guides/deployment/aws-lambda.mdx a/content/guides/deployment/aws-lambda.mdx
index bfda459f..4f283e10 100644
--- b/content/guides/deployment/aws-lambda.mdx
+++ a/content/guides/deployment/aws-lambda.mdx
@@ -110,6 +110,7 @@ In this guide, we will deploy a Bun HTTP server to AWS Lambda using a `Dockerfil
     		```bash title="terminal"
     		$ export ECR_URI=$(aws ecr create-repository --repository-name bun-lambda-demo --region us-east-1 --profile my-sso-app --query 'repository.repositoryUri' --output text)
     		$ echo $ECR_URI
+			[id].dkr.ecr.us-east-1.amazonaws.com/bun-lambda-demo
     		```
     	</Callout>
 
@@ -120,7 +121,7 @@ In this guide, we will deploy a Bun HTTP server to AWS Lambda using a `Dockerfil
     	Log in to the ECR repository:
 
     	```bash title="terminal"
-    	aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin $ECR_URI
+    	$ aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin $ECR_URI
     	
     	Login Succeeded
     	```
@@ -129,7 +130,7 @@ In this guide, we will deploy a Bun HTTP server to AWS Lambda using a `Dockerfil
     		If using a profile, use the `--profile` flag:
 
     		```bash title="terminal"
-    		aws ecr get-login-password --region us-east-1 --profile my-sso-app | docker login --username AWS --password-stdin $ECR_URI
+    		$ aws ecr get-login-password --region us-east-1 --profile my-sso-app | docker login --username AWS --password-stdin $ECR_URI
     		```
 
     	</Callout>
diff --git b/content/guides/deployment/digital-ocean.mdx a/content/guides/deployment/digital-ocean.mdx
index 42c32b11..646668be 100644
--- b/content/guides/deployment/digital-ocean.mdx
+++ a/content/guides/deployment/digital-ocean.mdx
@@ -39,10 +39,10 @@ In this guide, we will deploy a Bun HTTP server to DigitalOcean using a `Dockerf
     		<Tab value="Through the DigitalOcean CLI">
 
     			```bash title="terminal"
-    			doctl registry create bun-digitalocean-demo
+    			$ doctl registry create bun-digitalocean-demo
     			
     			Name                     Endpoint                                           Region slug
-    			$ bun-digitalocean-demo    registry.digitalocean.com/bun-digitalocean-demo    sfo2
+    			bun-digitalocean-demo    registry.digitalocean.com/bun-digitalocean-demo    sfo2
     			```
 
     		</Tab>
@@ -109,7 +109,7 @@ In this guide, we will deploy a Bun HTTP server to DigitalOcean using a `Dockerf
     	Before building and pushing the Docker image, authenticate Docker with the DigitalOcean Container Registry:
 
     	```bash title="terminal"
-    	doctl registry login
+    	$ doctl registry login
     	
     	Successfully authenticated with registry.digitalocean.com
     	```
diff --git b/content/guides/deployment/google-cloud-run.mdx a/content/guides/deployment/google-cloud-run.mdx
index 2342d801..b6ca1866 100644
--- b/content/guides/deployment/google-cloud-run.mdx
+++ a/content/guides/deployment/google-cloud-run.mdx
@@ -20,14 +20,16 @@ In this guide, we will deploy a Bun HTTP server to Google Cloud Run using a `Doc
 ---
 
 <Steps> 
-<Step title={<span>Initialize <code>gcloud</code> by select/creating a project</span>}>
+<Step>
+
+### Initialize `gcloud` by select/creating a project
 
 Make sure that you've initialized the Google Cloud CLI. This command logs you in, and prompts you to either select an existing project or create a new one.
 
 For more help with the Google Cloud CLI, see the [official documentation](https://docs.cloud.google.com/sdk/gcloud/reference/init).
 
 ```bash title="terminal"
-gcloud init
+$ gcloud init
 
 Welcome! This command will take you through the configuration of gcloud.
 
@@ -68,7 +70,7 @@ my-bun-app-... [PROJECT_NUMBER]
 List your available billing accounts and link one to your project:
 
 ```bash title="terminal"
-gcloud billing accounts list
+$ gcloud billing accounts list
 
 ACCOUNT_ID            NAME                OPEN  MASTER_ACCOUNT_ID
 [BILLING_ACCOUNT_ID]  My Billing Account  True
@@ -77,7 +79,7 @@ ACCOUNT_ID            NAME                OPEN  MASTER_ACCOUNT_ID
 Link your billing account to your project. Replace `[BILLING_ACCOUNT_ID]` with the ID of your billing account.
 
 ```bash title="terminal"
-gcloud billing projects link $PROJECT_ID --billing-account=[BILLING_ACCOUNT_ID]
+$ gcloud billing projects link $PROJECT_ID --billing-account=[BILLING_ACCOUNT_ID]
 
 billingAccountName: billingAccounts/[BILLING_ACCOUNT_ID]
 billingEnabled: true
@@ -92,10 +94,10 @@ projectId: my-bun-app-...
 Activate the necessary services and grant Cloud Build permissions:
 
 ```bash title="terminal"
-gcloud services enable run.googleapis.com cloudbuild.googleapis.com
-gcloud projects add-iam-policy-binding $PROJECT_ID \
-  --member=serviceAccount:$PROJECT_NUMBER-compute@developer.gserviceaccount.com \
-  --role=roles/run.builder
+$ gcloud services enable run.googleapis.com cloudbuild.googleapis.com
+$ gcloud projects add-iam-policy-binding $PROJECT_ID \
+    --member=serviceAccount:$PROJECT_NUMBER-compute@developer.gserviceaccount.com \
+    --role=roles/run.builder
 ```
 
 <Callout type="info">  
@@ -131,8 +133,7 @@ CMD ["bun", "index.ts"]
 <Callout type="info">
   Make sure that the start command corresponds to your application's entry point. This can also be `CMD ["bun", "run", "start"]` if you have a start script in your `package.json`.
 
-This image installs dependencies and runs your app with Bun inside a container. If your app doesn't have dependencies, you can omit the `RUN bun install --production --frozen-lockfile` line.
-
+  This image installs dependencies and runs your app with Bun inside a container. If your app doesn't have dependencies, you can omit the `RUN bun install --production --frozen-lockfile` line.
 </Callout>
 
 Create a new `.dockerignore` file in the root of your project. This file contains the files and directories that should be _excluded_ from the container image, such as `node_modules`. This makes your builds faster and smaller:
@@ -162,7 +163,7 @@ Make sure you're in the directory containing your `Dockerfile`, then deploy dire
 </Callout>
 
 ```bash title="terminal"
-gcloud run deploy my-bun-app --source . --region=us-west1 --allow-unauthenticated
+$ gcloud run deploy my-bun-app --source . --region=us-west1 --allow-unauthenticated
 
 Deploying from source requires an Artifact Registry Docker repository to store built containers. A repository named
 [cloud-run-source-deploy] in region [us-west1] will be created.
diff --git b/content/guides/deployment/vercel.mdx a/content/guides/deployment/vercel.mdx
index fffc4c51..fd6ee486 100644
--- b/content/guides/deployment/vercel.mdx
+++ a/content/guides/deployment/vercel.mdx
@@ -85,8 +85,7 @@ mode: center
 
     	```ts title="index.ts"
     	console.log("runtime", process.versions.bun);
-    	
-    	runtime 1.3.3
+    	// => runtime $BUN_LATEST_VERSION
     	```
 
     	[See the Vercel Bun Runtime documentation for feature support →](https://vercel.com/docs/functions/runtimes/bun#feature-support)
diff --git b/content/guides/ecosystem/gel.mdx a/content/guides/ecosystem/gel.mdx
index ec0be932..eccdde29 100644
--- b/content/guides/ecosystem/gel.mdx
+++ a/content/guides/ecosystem/gel.mdx
@@ -14,8 +14,8 @@ First, [install Gel](https://docs.geldata.com/learn/installation) if you haven't
 $ curl https://www.geldata.com/sh --proto "=https" -sSf1 | sh
 ```
 
-```sh tab="Windows terminal"
-irm https://www.geldata.com/ps1 | iex
+```ps1 tab="Windows terminal"
+> irm https://www.geldata.com/ps1 | iex
 ```
 
 ```sh tab="Homebrew terminal"
@@ -37,7 +37,7 @@ $ bun init -y
 We'll use the Gel CLI to initialize a Gel instance for our project. This creates a `gel.toml` file in our project root.
 
 ```sh title="terminal"
-gel project init
+$ gel project init
 
 No `gel.toml` found in `/Users/colinmcd94/Documents/bun/fun/examples/my-gel-app` or above
 Do you want to initialize a new project? [Y/n]
@@ -68,7 +68,7 @@ To connect to my_gel_app, run `gel`
 To see if the database is running, let's open a REPL and run a query.
 
 ```sh title="terminal"
-gel
+$ gel
 gel> select 1 + 1;
 
 2
@@ -77,6 +77,7 @@ gel> select 1 + 1;
 Then run `\quit` to exit the REPL.
 
 ```sh title="terminal"
+$ gel
 gel> \quit
 ```
 
@@ -108,13 +109,13 @@ module default {
 Then generate and apply an initial migration.
 
 ```sh title="terminal"
-gel migration create
+$ gel migration create
 
 Created /Users/colinmcd94/Documents/bun/fun/examples/my-gel-app/dbschema/migrations/00001.edgeql, id: m1uwekrn4ni4qs7ul7hfar4xemm5kkxlpswolcoyqj3xdhweomwjrq
 ```
 
 ```sh title="terminal"
-gel migrate
+$ gel migrate
 
 Applied m1uwekrn4ni4qs7ul7hfar4xemm5kkxlpswolcoyqj3xdhweomwjrq (00001.edgeql)
 ```
diff --git b/content/guides/ecosystem/nextjs.mdx a/content/guides/ecosystem/nextjs.mdx
index a8b3cceb..060105a4 100644
--- b/content/guides/ecosystem/nextjs.mdx
+++ a/content/guides/ecosystem/nextjs.mdx
@@ -58,32 +58,13 @@ mode: center
 
 Next.js applications on Bun can be deployed to various platforms.
 
-<Columns cols={3}>
-  <Card title="Vercel" href="/guides/deployment/vercel" icon="/icons/ecosystem/vercel.svg">
-    Deploy on Vercel
-  </Card>
-  <Card title="Railway" href="/guides/deployment/railway" icon="/icons/ecosystem/railway.svg">
-    Deploy on Railway
-  </Card>
-  <Card title="DigitalOcean" href="/guides/deployment/digital-ocean" icon="/icons/ecosystem/digitalocean.svg">
-    Deploy on DigitalOcean
-  </Card>
-  <Card title="AWS Lambda" href="/guides/deployment/aws-lambda" icon="/icons/ecosystem/aws.svg">
-    Deploy on AWS Lambda
-  </Card>
-  <Card title="Google Cloud Run" href="/guides/deployment/google-cloud-run" icon="/icons/ecosystem/gcp.svg">
-    Deploy on Google Cloud Run
-  </Card>
-  <Card title="Render" href="/guides/deployment/render" icon="/icons/ecosystem/render.svg">
-    Deploy on Render
-  </Card>
-</Columns>
+<include>../../_snippets/guides/deploy.mdx</include>
 
 ---
 
 ## Templates
 
-<Columns cols={2}>
+<Cards>
   <Card
     title="Bun + Next.js Basic Starter"
     img="/images/templates/bun-nextjs-basic.png"
@@ -102,7 +83,7 @@ Next.js applications on Bun can be deployed to various platforms.
   >
     A full-stack todo application built with Bun, Next.js, and PostgreSQL.
   </Card>
-</Columns>
+</Cards>
 
 ---
 
diff --git b/content/guides/ecosystem/nuxt.mdx a/content/guides/ecosystem/nuxt.mdx
index 95481525..5429f19f 100644
--- b/content/guides/ecosystem/nuxt.mdx
+++ a/content/guides/ecosystem/nuxt.mdx
@@ -35,7 +35,7 @@ To start the dev server, run `bun --bun run dev` from the project root. This wil
 $ cd my-nuxt-app
 $ bun --bun run dev
 
-nuxt dev
+ $ nuxt dev
 Nuxi 3.6.5
 Nuxt 3.6.5 with Nitro 2.5.2
   > Local:    http://localhost:3000/
diff --git b/content/guides/ecosystem/pm2.mdx a/content/guides/ecosystem/pm2.mdx
index 74c23676..d7a9bb29 100644
--- b/content/guides/ecosystem/pm2.mdx
+++ a/content/guides/ecosystem/pm2.mdx
@@ -22,7 +22,7 @@ You can use PM2 with Bun in two ways: as a CLI option or in a configuration file
 To start your application with PM2 and Bun as the interpreter, open your terminal and run the following command:
 
 ```bash title="terminal"
-pm2 start --interpreter ~/.bun/bin/bun index.ts
+$ pm2 start --interpreter ~/.bun/bin/bun index.ts
 ```
 
 ---
@@ -47,7 +47,7 @@ module.exports = {
 After saving the file, you can start your application with PM2
 
 ```bash title="terminal"
-pm2 start pm2.config.js
+$ pm2 start pm2.config.js
 ```
 
 ---
diff --git b/content/guides/ecosystem/prisma.mdx a/content/guides/ecosystem/prisma.mdx
index e0fffed5..e5911907 100644
--- b/content/guides/ecosystem/prisma.mdx
+++ a/content/guides/ecosystem/prisma.mdx
@@ -1,7 +1,6 @@
 ---
 title: Prisma ORM with Bun
 fullTitle: Use Prisma with Bun
-mode: center
 ---
 
 <Callout type="info">
@@ -149,8 +148,7 @@ mode: center
 
     	```bash title="terminal"
     	$ bun run index.ts
-    	```
-    	```txt
+		
     	Created john-0.12802932895402364@example.com
     	There are 1 users in the database.
     	```
diff --git b/content/guides/ecosystem/qwik.mdx a/content/guides/ecosystem/qwik.mdx
index 99b002b6..ea9e377e 100644
--- b/content/guides/ecosystem/qwik.mdx
+++ a/content/guides/ecosystem/qwik.mdx
@@ -79,7 +79,6 @@ $ bun create qwik
 ├──────────────────────────────────────────────────────╯
 │
 └  Happy coding! 🎉
-
 ```
 
 ---
diff --git b/content/guides/ecosystem/remix.mdx a/content/guides/ecosystem/remix.mdx
index 9b24bdc5..fb3d3f2c 100644
--- b/content/guides/ecosystem/remix.mdx
+++ a/content/guides/ecosystem/remix.mdx
@@ -69,19 +69,17 @@ To build and start your app, run `bun run build`
 
 ```sh title="terminal"
 $ bun run build
-
-$ remix build
-info  building... (NODE_ENV=production)
-info  built (158ms)
+  $ remix build
+  info  building... (NODE_ENV=production)
+  info  built (158ms)
 ```
 
 Then `bun run start` from the project root.
 
 ```sh title="terminal"
 $ bun start
-
-$ remix-serve ./build/index.js
-[remix-serve] http://localhost:3000 (http://192.168.86.237:3000)
+  $ remix-serve ./build/index.js
+  [remix-serve] http://localhost:3000 (http://192.168.86.237:3000)
 ```
 
 ---
diff --git b/content/guides/ecosystem/solidstart.mdx a/content/guides/ecosystem/solidstart.mdx
index f2230557..23a151dc 100644
--- b/content/guides/ecosystem/solidstart.mdx
+++ a/content/guides/ecosystem/solidstart.mdx
@@ -42,8 +42,7 @@ Then run the development server with `bun dev`.
 
 ```sh title="terminal"
 $ bun dev
-
-$ vinxi dev
+\$ vinxi dev
 vinxi v0.5.8
 vinxi starting dev server
 
diff --git b/content/guides/ecosystem/tanstack-start.mdx a/content/guides/ecosystem/tanstack-start.mdx
index 9ffdada8..38c4bbd5 100644
--- b/content/guides/ecosystem/tanstack-start.mdx
+++ a/content/guides/ecosystem/tanstack-start.mdx
@@ -66,7 +66,7 @@ To host your TanStack Start app, you can use [Nitro](https://nitro.build/) or a
 
         ```sh title="terminal"
         $ bun add nitro
-   ```
+        ```
 
       </Step>
       <Step title={<span>Update your <code>vite.config.ts</code> file</span>}>
@@ -85,7 +85,7 @@ To host your TanStack Start app, you can use [Nitro](https://nitro.build/) or a
         });
 
         export default config;
-   ```
+        ```
 
         <Callout type="info">
           The `bun` preset is optional, but it configures the build output specifically for Bun's runtime.
@@ -106,7 +106,7 @@ To host your TanStack Start app, you can use [Nitro](https://nitro.build/) or a
               "start": "bun run .output/server/index.mjs" // [!code ++]
             }
           }
-   ```
+        ```
 
         <Callout type="info">
           You do **not** need the custom `start` script when deploying to Vercel.
@@ -139,7 +139,7 @@ To host your TanStack Start app, you can use [Nitro](https://nitro.build/) or a
               }),
             ],
           });
-   ```
+          ```
         </Callout>
       </Step>
     </Steps>
@@ -713,7 +713,7 @@ To host your TanStack Start app, you can use [Nitro](https://nitro.build/) or a
           log.error(`Failed to start server: ${String(error)}`)
           process.exit(1)
         })
-   ```
+       ```
 
       </Step>
       <Step>
@@ -728,7 +728,7 @@ To host your TanStack Start app, you can use [Nitro](https://nitro.build/) or a
             "start": "bun run server.ts" // [!code ++]
           }
         }
-   ```
+       ```
 
       </Step>
       <Step>
@@ -739,7 +739,7 @@ To host your TanStack Start app, you can use [Nitro](https://nitro.build/) or a
         ```sh title="terminal"
         $ bun run build
         $ bun run start
-   ```
+        ```
 
         The server will start on port 3000 by default (configurable via `PORT` environment variable).
 
@@ -749,32 +749,13 @@ To host your TanStack Start app, you can use [Nitro](https://nitro.build/) or a
   </Tab>
 </Tabs>
 
-<Columns cols={3}>
-  <Card title="Vercel" href="/guides/deployment/vercel" icon="/icons/ecosystem/vercel.svg">
-    Deploy on Vercel
-  </Card>
-  <Card title="Render" href="/guides/deployment/render" icon="/icons/ecosystem/render.svg">
-    Deploy on Render
-  </Card>
-  <Card title="Railway" href="/guides/deployment/railway" icon="/icons/ecosystem/railway.svg">
-    Deploy on Railway
-  </Card>
-  <Card title="DigitalOcean" href="/guides/deployment/digital-ocean" icon="/icons/ecosystem/digitalocean.svg">
-    Deploy on DigitalOcean
-  </Card>
-  <Card title="AWS Lambda" href="/guides/deployment/aws-lambda" icon="/icons/ecosystem/aws.svg">
-    Deploy on AWS Lambda
-  </Card>
-  <Card title="Google Cloud Run" href="/guides/deployment/google-cloud-run" icon="/icons/ecosystem/gcp.svg">
-    Deploy on Google Cloud Run
-  </Card>
-</Columns>
+<include>../../_snippets/guides/deploy.mdx</include>
 
 ---
 
 ## Templates
 
-<Columns cols={2}>
+<Cards>
   <Card
     title="Todo App with Tanstack + Bun"
     img="/images/templates/bun-tanstack-todo.png"
@@ -802,7 +783,7 @@ To host your TanStack Start app, you can use [Nitro](https://nitro.build/) or a
   >
     The basic TanStack starter using the Bun runtime and Bun's file APIs.
   </Card>
-</Columns>
+</Cards>
 
 ---
 
diff --git b/content/guides/ecosystem/upstash.mdx a/content/guides/ecosystem/upstash.mdx
index 3532a260..28cc6a07 100644
--- b/content/guides/ecosystem/upstash.mdx
+++ a/content/guides/ecosystem/upstash.mdx
@@ -84,8 +84,7 @@ mode: center
     	// Get the updated value
     	counter = await redis.get("counter");
     	console.log(counter);
-    	
-    	1
+    	// => 1
     	```
 
     	The Redis client automatically handles connections in the background. No need to manually connect or disconnect for basic operations.
diff --git b/content/guides/index.mdx a/content/guides/index.mdx
index 107c894c..07a4fff2 100644
--- b/content/guides/index.mdx
+++ a/content/guides/index.mdx
@@ -5,6 +5,112 @@ contextual: false
 mode: center
 ---
 
-import { GuidesList } from "/snippets/guides.jsx";
+## Featured
 
-<GuidesList />
+import { Card as FancyCard, Cards as FancyCards } from "@/components/ui/card";
+
+<FancyCards className="grid-cols-3">
+  <FancyCard title="Use Tanstack Start with Bun" href="/guides/ecosystem/tanstack-start" cta="View Guide" />
+  <FancyCard title="Use Next.js with Bun" href="/guides/ecosystem/nextjs" cta="View Guide" />
+  <FancyCard title="Build a frontend using Vite and Bun" href="/guides/ecosystem/vite" cta="View Guide" />
+  <FancyCard title="Install TypeScript declarations for Bun" href="/guides/runtime/typescript" cta="View Guide" />
+  <FancyCard title="Write a simple HTTP server" href="/guides/http/simple" cta="View Guide" />
+  <FancyCard title="Build a simple WebSocket server" href="/guides/websocket/simple" cta="View Guide" />
+</FancyCards>
+
+## All Guides
+
+import { readdirSync, readFileSync } from 'node:fs';
+import { join } from 'node:path';
+import { Card, Cards } from 'fumadocs-ui/components/card';
+
+export const GuidesCategory = ({ category }) => {
+  const guidesDir = join(process.cwd(), 'content/guides', category);
+  const files = readdirSync(guidesDir).filter(f => f.endsWith('.mdx'));
+  
+  const pages = files.map(file => {
+    const content = readFileSync(join(guidesDir, file), 'utf-8');
+    
+    const frontmatterMatch = content.match(/^---\n([\s\S]*?)\n---/);
+    const frontmatter = frontmatterMatch ? frontmatterMatch[1] : '';
+    
+    const titleMatch = frontmatter.match(/^title:\s*['"]?(.+?)['"]?\s*$/m);
+    const fullTitleMatch = frontmatter.match(/^fullTitle:\s*['"]?(.+?)['"]?\s*$/m);
+    
+    const pageTitle = titleMatch ? titleMatch[1] : file.replace('.mdx', '');
+    const pageFullTitle = fullTitleMatch ? fullTitleMatch[1] : pageTitle;
+    
+    return {
+      title: pageFullTitle ||pageTitle,
+      url: `/guides/${category}/${file.replace('.mdx', '')}`
+    };
+  }).sort((a, b) => a.title.localeCompare(b.title));
+
+  return (
+    <Cards className="grid-cols-3">
+      {pages.map((page) => (
+        <Card 
+          key={page.url} 
+          title={page.title} 
+          href={page.url}
+        />
+      ))}
+    </Cards>
+  );
+};
+
+### Deployment
+
+<GuidesCategory category="deployment" />
+
+### Binary Data
+
+<GuidesCategory category="binary" />
+
+### Ecosystem & Frameworks
+
+<GuidesCategory category="ecosystem" />
+
+### File System - Read
+
+<GuidesCategory category="read-file" />
+
+### File System - Write
+
+<GuidesCategory category="write-file" />
+
+### HTML Processing
+
+<GuidesCategory category="html-rewriter" />
+
+### HTTP & Networking
+
+<GuidesCategory category="http" />
+
+### Package Manager
+
+<GuidesCategory category="install" />
+
+### Process & System
+
+<GuidesCategory category="process" />
+
+### Runtime & Debugging
+
+<GuidesCategory category="runtime" />
+
+### Streams
+
+<GuidesCategory category="streams" />
+
+### Test Runner
+
+<GuidesCategory category="test" />
+
+### Utilities
+
+<GuidesCategory category="util" />
+
+### WebSocket
+
+<GuidesCategory category="websocket" />
diff --git b/content/guides/install/from-npm-install-to-bun-install.mdx a/content/guides/install/from-npm-install-to-bun-install.mdx
index 5fd41dd1..372dc4de 100644
--- b/content/guides/install/from-npm-install-to-bun-install.mdx
+++ a/content/guides/install/from-npm-install-to-bun-install.mdx
@@ -48,7 +48,8 @@ $ bun my-script
 $ bun run my-script
 
 # Run an executable in node_modules/.bin:
-$ bun my-executable # such as tsc, esbuild, etc.
+$ bun my-executable
+# such as tsc, esbuild, etc.
 $ bun run my-executable
 
 # Run a JavaScript/TypeScript file:
@@ -150,7 +151,6 @@ To list installed packages, you can use `bun pm ls`. This will list all the pack
 ```sh title="terminal"
 # List top-level installed packages:
 $ bun pm ls
-
 my-pkg node_modules (781)
 ├── @types/node@20.16.5
 ├── @types/react@18.3.8
@@ -163,7 +163,6 @@ my-pkg node_modules (781)
 ```sh title="terminal"
 # List all installed packages:
 $ bun pm ls -a
-
 my-pkg node_modules
 ├── @alloc/quick-lru@5.2.0
 ├── @isaacs/cliui@8.0.2
@@ -183,7 +182,6 @@ To create a package tarball, you can use `bun pm pack`. This will create a tarba
 ```sh title="terminal"
 # Create a tarball
 $ bun pm pack
-
 Total files: 46
 Shasum: 2ee19b6f0c6b001358449ca0eadead703f326216
 Integrity: sha512-ZV0lzWTEkGAMz[...]Gl4f8lA9sl97g==
@@ -218,5 +216,5 @@ You can install packages globally using `bun i -g <package>`. This will install
 $ bun i -g eslint
 
 # Run a globally-installed package without the `bun run` prefix
-eslint --init
+$ eslint --init
 ```
diff --git b/content/guides/install/registry-scope.mdx a/content/guides/install/registry-scope.mdx
index 88ddcc0b..9a204350 100644
--- b/content/guides/install/registry-scope.mdx
+++ a/content/guides/install/registry-scope.mdx
@@ -23,7 +23,6 @@ To configure a registry for a particular npm scope:
 
 # as an object with token
 "@myorg3" = { token = "$npm_token", url = "https://registry.myorg.com/" }
-
 ```
 
 ---
diff --git b/content/guides/runtime/build-time-constants.mdx a/content/guides/runtime/build-time-constants.mdx
index 29e47553..0a63d325 100644
--- b/content/guides/runtime/build-time-constants.mdx
+++ a/content/guides/runtime/build-time-constants.mdx
@@ -63,33 +63,28 @@ await Bun.build({
 
 Embed version and build metadata directly into your executable:
 
-<Tabs items={['JavaScript', 'CLI']} groupId="js/cli">
-  <Tab value="JavaScript">
-    ```ts title="src/version.ts"
-    // These constants are replaced at build time
-    declare const BUILD_VERSION: string;
-    declare const BUILD_TIME: string;
-    declare const GIT_COMMIT: string;
-
-    export function getVersion() {
-      return {
-        version: BUILD_VERSION,
-        buildTime: BUILD_TIME,
-        commit: GIT_COMMIT,
-      };
-    }
-   ```
-  </Tab>
-  <Tab value="CLI">
-    ```sh title="terminal" Build command
-    $ bun build --compile \
-      --define BUILD_VERSION='"1.2.3"' \
-      --define BUILD_TIME='"2024-01-15T10:30:00Z"' \
-      --define GIT_COMMIT='"abc123"' \
-      src/cli.ts --outfile mycli
-   ```
-  </Tab>
-</Tabs>
+```ts tab="src/version.ts"
+// These constants are replaced at build time
+declare const BUILD_VERSION: string;
+declare const BUILD_TIME: string;
+declare const GIT_COMMIT: string;
+
+export function getVersion() {
+  return {
+    version: BUILD_VERSION,
+    buildTime: BUILD_TIME,
+    commit: GIT_COMMIT,
+  };
+}
+```
+
+```sh tab="Build command"
+$ bun build --compile \
+    --define BUILD_VERSION='"1.2.3"' \
+    --define BUILD_TIME='"2024-01-15T10:30:00Z"' \
+    --define GIT_COMMIT='"abc123"' \
+    src/cli.ts --outfile mycli
+```
 
 ### Feature flags
 
@@ -166,10 +161,10 @@ Generate build-time constants from shell commands:
 ```sh
 # Use git to get current commit and timestamp
 $ bun build --compile \
-  --define BUILD_VERSION="\"$(git describe --tags --always)\"" \
-  --define BUILD_TIME="\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"" \
-  --define GIT_COMMIT="\"$(git rev-parse HEAD)\"" \
-  src/cli.ts --outfile mycli
+    --define BUILD_VERSION="\"$(git describe --tags --always)\"" \
+    --define BUILD_TIME="\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"" \
+    --define GIT_COMMIT="\"$(git rev-parse HEAD)\"" \
+    src/cli.ts --outfile mycli
 ```
 
 ### Build automation script
diff --git b/content/guides/runtime/cicd.mdx a/content/guides/runtime/cicd.mdx
index bf22f065..e9de250b 100644
--- b/content/guides/runtime/cicd.mdx
+++ a/content/guides/runtime/cicd.mdx
@@ -37,7 +37,7 @@ jobs:
       # ...
       - uses: oven-sh/setup-bun@v2
         with: # [!code ++]
-          bun-version: 1.3.3 # or "latest", "canary", <sha> # [!code ++]
+          bun-version: $BUN_LATEST_VERSION # or "latest", "canary", <sha> # [!code ++]
 ```
 
 ---
diff --git b/content/guides/runtime/codesign-macos-executable.mdx a/content/guides/runtime/codesign-macos-executable.mdx
index cbe257d5..58a41b64 100644
--- b/content/guides/runtime/codesign-macos-executable.mdx
+++ a/content/guides/runtime/codesign-macos-executable.mdx
@@ -16,8 +16,7 @@ $ bun build --compile ./path/to/entry.ts --outfile myapp
 List your available signing identities. One of these will be your signing identity that you pass to the `codesign` command. This command requires macOS.
 
 ```sh title="terminal"
-security find-identity -v -p codesigning
-
+$ security find-identity -v -p codesigning
 1. XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX "Developer ID Application: Your Name (ZZZZZZZZZZ)"
    1 valid identities found
 ```
diff --git b/content/guides/runtime/set-env.mdx a/content/guides/runtime/set-env.mdx
index 18c12c64..fade4263 100644
--- b/content/guides/runtime/set-env.mdx
+++ a/content/guides/runtime/set-env.mdx
@@ -39,7 +39,7 @@ $ FOO=helloworld bun run dev
 $ set FOO=helloworld && bun run dev
 
 # Using PowerShell
-$env:FOO="helloworld"; bun run dev
+$ $env:FOO="helloworld"; bun run dev
 ```
 
 ---
diff --git b/content/guides/test/coverage-threshold.mdx a/content/guides/test/coverage-threshold.mdx
index 802d7559..22747491 100644
--- b/content/guides/test/coverage-threshold.mdx
+++ a/content/guides/test/coverage-threshold.mdx
@@ -9,8 +9,6 @@ Bun's test runner supports built-in code coverage reporting via the `--coverage`
 ```sh title="terminal"
 $ bun test --coverage
 
-bun test v$BUN_LATEST_VERSION (9c68abdb)
-
 test.test.ts:
 ✓ math > add [0.71ms]
 ✓ math > multiply [0.03ms]
@@ -45,8 +43,6 @@ If your test suite does not meet this threshold, `bun test` will exit with a non
 ```sh title="terminal"
 $ bun test --coverage
 
-bun test v$BUN_LATEST_VERSION (9c68abdb)
-
 <test output>
 $ echo $?
 1 # this is the exit code of the previous command
diff --git b/content/guides/test/run-tests.mdx a/content/guides/test/run-tests.mdx
index 5c1cd476..37b289d1 100644
--- b/content/guides/test/run-tests.mdx
+++ a/content/guides/test/run-tests.mdx
@@ -23,7 +23,6 @@ Here's what the output of a typical test run looks like. In this case, there are
 
 ```sh title="terminal"
 $ bun test
-
 bun test v$BUN_LATEST_VERSION (9c68abdb)
 
 test.test.js:
@@ -50,7 +49,6 @@ To only run certain test files, pass a positional argument to `bun test`. The ru
 
 ```sh title="terminal"
 $ bun test test3
-
 bun test v$BUN_LATEST_VERSION (9c68abdb)
 
 test3.test.js:
@@ -89,7 +87,6 @@ Adding `-t add` will only run tests with "add" in the name. This works with test
 
 ```sh title="terminal"
 $ bun test -t add
-
 bun test v$BUN_LATEST_VERSION (9c68abdb)
 
 test.test.js:
diff --git b/content/guides/test/skip-tests.mdx a/content/guides/test/skip-tests.mdx
index c311c57c..4aebd9b1 100644
--- b/content/guides/test/skip-tests.mdx
+++ a/content/guides/test/skip-tests.mdx
@@ -20,7 +20,6 @@ Running `bun test` will not execute this test. It will be marked as skipped in t
 
 ```sh title="terminal"
 $ bun test
-
 bun test v$BUN_LATEST_VERSION (9c68abdb)
 
 test.test.ts:
diff --git b/content/guides/test/snapshot.mdx a/content/guides/test/snapshot.mdx
index 023597a7..a3e3e649 100644
--- b/content/guides/test/snapshot.mdx
+++ a/content/guides/test/snapshot.mdx
@@ -20,7 +20,6 @@ The first time this test is executed, Bun will evaluate the value passed into `e
 
 ```sh title="terminal"
 $ bun test test/snap
-
 bun test v$BUN_LATEST_VERSION (9c68abdb)
 
 test/snap.test.ts:
@@ -64,8 +63,8 @@ Later, when this test file is executed again, Bun will read the snapshot file an
 
 ```sh title="terminal"
 $ bun test
-
 bun test v$BUN_LATEST_VERSION (9c68abdb)
+
 test/snap.test.ts:
 ✓ snapshot [1.05ms]
 
@@ -81,8 +80,8 @@ To update snapshots, use the `--update-snapshots` flag.
 
 ```sh title="terminal"
 $ bun test --update-snapshots
-
 bun test v$BUN_LATEST_VERSION (9c68abdb)
+
 test/snap.test.ts:
 ✓ snapshot [0.86ms]
 
diff --git b/content/guides/test/todo-tests.mdx a/content/guides/test/todo-tests.mdx
index 810760b7..d9c25efd 100644
--- b/content/guides/test/todo-tests.mdx
+++ a/content/guides/test/todo-tests.mdx
@@ -19,7 +19,6 @@ The output of `bun test` indicates how many `todo` tests were encountered.
 
 ```sh title="terminal"
 $ bun test
-
 bun test v$BUN_LATEST_VERSION (9c68abdb)
 
 test.test.ts:
@@ -52,7 +51,6 @@ If an implementation is provided, it will not be run unless the `--todo` flag is
 
 ```sh title="terminal"
 $ bun test --todo
-
 bun test v$BUN_LATEST_VERSION (9c68abdb)
 
 my.test.ts:
diff --git b/content/guides/test/update-snapshots.mdx a/content/guides/test/update-snapshots.mdx
index eb97b5c6..0f15a187 100644
--- b/content/guides/test/update-snapshots.mdx
+++ a/content/guides/test/update-snapshots.mdx
@@ -31,7 +31,6 @@ To regenerate snapshots, use the `--update-snapshots` flag.
 
 ```sh title="terminal"
 $ bun test --update-snapshots
-
 bun test v$BUN_LATEST_VERSION (9c68abdb)
 
 test/snap.test.ts:
diff --git b/content/guides/util/main.mdx a/content/guides/util/main.mdx
index c6c10519..59bf3896 100644
--- b/content/guides/util/main.mdx
+++ a/content/guides/util/main.mdx
@@ -18,15 +18,10 @@ import "./foo.ts";
 
 The printed path corresponds to the file that is executed with `bun run`.
 
-```sh title="terminal"
+```sh
 $ bun run index.ts
-
 /path/to/index.ts
-```
-
-```sh title="terminal"
 $ bun run foo.ts
-
 /path/to/foo.ts
 ```
 
diff --git b/content/guides/util/version.mdx a/content/guides/util/version.mdx
index 4da39ce4..365df233 100644
--- b/content/guides/util/version.mdx
+++ a/content/guides/util/version.mdx
@@ -7,7 +7,7 @@ mode: center
 Get the current version of Bun in a semver format.
 
 ```ts title="index.ts"
-Bun.version; // => "1.3.3"
+Bun.version; // => "$BUN_LATEST_VERSION"
 ```
 
 ---
